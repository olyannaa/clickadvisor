---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"Handling
topic: handling-updates-and-deletes-in-clickhouse-clickhouse
ch_version_introduced: '1.182'
last_updated: '2026-09-07'
chunk_index: 5
total_chunks_in_doc: 11
---

to a value of `1` (or `2` if we also want to wait till all replicas are also updated): ``` 1SET mutations_sync = 1 ``` Copy command Now our update query will wait for the mutation to complete:

```
1ALTER TABLE table
2    UPDATE col1 = 'bye' WHERE col2 > 0
3
40 rows in set. Elapsed: 1.182 sec.
```
Copy command
Note how it took 1 second for this query to finish while ClickHouse waited for the background mutation to complete. Note this parameter also applies to Lightweight Deletes.

## Updating an Entire Table \#

In some cases, users need to update the values of an entire column. Initially, users may try to achieve this by using the `ALTER TABLE` query without a `WHERE` clause. This, however, fails, as shown below:

```
1ALTER TABLE table UPDATE col1 = 'bye';
2
3Syntax error: failed at position 38 (end of query):
4ALTER TABLE table UPDATE col1 = 'bye';
```
Copy command
ClickHouse won't let you update an entire table because updates are heavy. One way to force ClickHouse to accept this operation is with an always\-true filter:

```
1ALTER TABLE table
2    UPDATE col1 = 'bye' WHERE true
```
Copy command
However, a more optimal approach is to create a new column with the new value as the default and then switch the old and new columns. For example:

```
1ALTER TABLE table ADD COLUMN col1_new String DEFAULT 'global hi';
2
3ALTER TABLE table
4    RENAME COLUMN col1 TO col1_old,
5    RENAME COLUMN col1_new TO col1,
6    DROP COLUMN col1_old;
```
Copy command
We use the default value of the `col1_new` column to specify what we want to use as the updated value. This is safe and much more efficient since we skip the heavy mutation operation here.

## Updating and Deleting using JOINs \#

Sometimes, we need to delete or update rows based on relations; thus, we have to join tables. This is best achieved in ClickHouse using the [Join table engine](https://clickhouse.com/docs/en/engines/table-engines/special/join/) and [joinGet](https://clickhouse.com/docs/en/sql-reference/functions/other-functions/#joinget) function. Suppose we have two tables \- one with all pageviews and the other with all logins tracked:
