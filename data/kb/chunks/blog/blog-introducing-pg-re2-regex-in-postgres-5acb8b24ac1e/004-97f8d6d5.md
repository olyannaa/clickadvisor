---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"Introducing
topic: introducing-pg-re2-fast-re2-powered-regular-expressions-in-postgres-clickhouse
ch_version_introduced: '0.4'
last_updated: '2026-09-07'
chunk_index: 4
total_chunks_in_doc: 8
---

for simple regexen: ``` 1SELECT id, by FROM log.entries WHERE by ~ 'Click' ORDER BY id; ``` Copy command ``` 1id | by 2----+------------ 3 1 | ClickHouse 4 5 | MouseClick 5 (2 rows) ``` Copy command

> If you'd like to follow along, the following examples use this ClickHouse
> table:
> 
> 
> 
> ```
> 1CREATE TABLE entries (
> 2    id UInt32 NOT NULL,
> 3    by String NOT NULL
> 4) ENGINE = MergeTree ORDER BY (id);
> 5
> 6INSERT INTO entries VALUES
> 7    ( 1, 'ClickHouse' ),
> 8    ( 2, 'HouseQuake' ),
> 9    ( 3, 'HouseQuake' ),
> 10    ( 4, 'QuakerOats' ),
> 11    ( 5, 'MouseClick' );
> ```
> Copy command
> And this pg\_clickhouse foreign table configuration in Postgres:
> 
> 
> 
> ```
> 1CREATE EXTENSION pg_clickhouse;
> 2CREATE EXTENSION re2;
> 3CREATE SERVER ch FOREIGN DATA WRAPPER clickhouse_fdw OPTIONS(driver 'binary');
> 4CREATE USER MAPPING FOR CURRENT_USER SERVER ch;
> 5CREATE SCHEMA log;
> 6IMPORT FOREIGN SCHEMA "default" FROM SERVER ch INTO log;
> ```
> Copy command

It's also useful for simple anchored expressions like `^` and `$`:

```
1SELECT id, by FROM log.entries WHERE by ~ '^House' ORDER BY id;
```
Copy command

```
1id |     by     
2----+------------
3  2 | HouseQuake
4  3 | HouseQuake
5(2 rows)
```
Copy command
But sometimes pg\_clickhouse returns unexpected results:

```
1SELECT id, by ~ 'Quake\b' FROM log.entries WHERE by ~ 'Quake\b';
```
Copy command

```
1id | ?column? 
2----+----------
3  2 | f
4  3 | f
5(2 rows)
```
Copy command
Here `by ~ 'Quake\b'` in the `WHERE` clause, which pushes down to ClickHouse,
returns true for two rows, but the same expression in the select list, which
executes in Postgres, surprisingly returns false! How could that be?

## Postgres POSIX vs. ClickHouse RE2 \#

This perplexing outcome derives from a difference between the two engines. In
ClickHouse, `\b` represents a word boundary, while in Postgres it represents a
backspace character. Thus the `WHERE` expression pushed down to ClickHouse
returns true, while the `SELECT` expression that executes in Postgres does
not.
