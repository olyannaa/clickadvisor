---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"Announcing
topic: announcing-adsb-exposed-interactive-visualization-and-analytics-on-ads-b-flight-data-with-clickhouse-clickhouse
ch_version_introduced: '40.7168'
last_updated: '2026-09-07'
chunk_index: 3
total_chunks_in_doc: 10
---

OpenStreetMap to create a typical geographic map. The main layer overlays the visualization on top of the background map. The visualization layer uses a `GridLayer` with a custom callback function, `createTile` which generates Canvas elements on the fly:

```
1L.GridLayer.ClickHouse = L.GridLayer.extend({
2	createTile: function(coords, done) {
3    	let tile = L.DomUtil.create('canvas', 'leaflet-tile');
4    	tile.width = 1024;
5    	tile.height = 1024;
6    	render(this.options.table, this.options.priority, coords, tile).then([object Object], => done(err, tile));
7    	return tile;
8	}
9});
10
11const layer_options = {
12	tileSize: 1024,
13	minZoom: 2,
14	maxZoom: 19,
15	minNativeZoom: 2,
16	maxNativeZoom: 16,
17	attribution: '(c) Alexey Milovidov, ClickHouse, Inc. (data: adsb.lol, adsbexchange.com)'
18};
```
Copy command
Each tile has a high resolution of 1024x1024 size to lower the number of requests to the database.

The rendering function performs a request to ClickHouse using its HTTP API with the JavaScript's fetch function:

```
1const query_id = `,[object Object],-,[object Object],-,[object Object],-,[object Object],-,[object Object],-,[object Object],`;
2const hosts = getHosts(key);
3const url = host => `,[object Object],/?user=website&default_format=RowBinary` +
4	`&query_id=,[object Object],&replace_running_query=1` +
5	`&param_table=,[object Object],&param_sampling=,[object Object],` +
6	`&param_z=,[object Object],&param_x=,[object Object],&param_y=,[object Object],`;
7
8progress_update_period = 1;
9const response = await Promise.race(hosts.map([object Object], => fetch(url(host), { method: 'POST', body: sql })));
```
Copy command
A user can edit the SQL query on the fly using a form on the page to adjust the visualization. This parameterized query accepts tile coordinates (x, y) and zoom level as parameters.

The query returns RGBA values of each pixel of the image in the RowBinary format (1024x1024 pixels, 1048576 rows, 4 bytes each, 4 MiB in total for each tile). It uses ZSTD compression in HTTP response as long as the browser supports it. It was a nice observation that ZSTD compression over raw pixels bitmap works better than PNG! (not surprising, though).

While the image data is often compressed several times, hundreds of megabytes still have to be transferred over the network. This is why the service can feel slow on bad Internet connections.
