---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"ClickHouse
topic: clickhouse-release-26-4-clickhouse
ch_version_introduced: '26.4'
last_updated: '2026-09-07'
chunk_index: 7
total_chunks_in_doc: 9
---

top of this, enabling more efficient filtering on JSON subcolumns. Let’s have a look at how this works with help from the [StatsBomb dataset](https://github.com/statsbomb/open-data/tree/master). Let’s get a subset of the data on our machine by running the following:

```
1git clone --filter=blob:none --sparse https://github.com/statsbomb/open-data.git
2cd open-data
3git sparse-checkout set data/events
```
Copy command
We’ll create the following table using clickhouse\-local:

```
1CREATE TABLE events (
2      match_id UInt32,
3      json JSON(id String, index UInt32),
4      INDEX vals JSONAllValues(json) TYPE text(tokenizer = 'ngrams') GRANULARITY 1
5  )
6  ENGINE = MergeTree
7  ORDER BY (match_id, json.index);
```
Copy command
And then insert the data:

```
1INSERT INTO events
2SELECT
3  toUInt32(replaceRegexpOne(_file, '\\.json$', '')) AS match_id,
4  json
5FROM file('open-data/data/events/*.json', JSONAsObject);
```
Copy command

```
112188949 rows in set. Elapsed: 1275.404 sec. Processed 12.19 million rows, 10.48 GB (9.56 thousand rows/s., 8.22 MB/s.)
2Peak memory usage: 1.87 GiB.
```
Copy command
Just for our understanding of how the index works, let’s have a look at what the `JSONAllValues` function returns:

```
1SELECT JSONAllValues(json) FROM events LIMIT 1
2FORMAT Vertical;
```
Copy command

```
1JSONAllValues(json): ['[36.4,21.7]','1.013174','000000b5-8156-429d-9088-e62a6ac2ea0d','2529','[36.8,20]','60','2','4','From Throw In','10958','Chris Smalling','5','Left Center Back','123','39','Manchester United','[\'5fbbde9b-74ab-48e9-9873-ef956db384de\',\'fd43cc18-c37b-438a-8a40-a8bb50e59469\']','18','39','Manchester United','00:15:18.727','43','Carry']
```
Copy command
The dataset has just over 12 million records, which isn’t really enough to see the impact of the index, so we’ll duplicate the data a bunch of times:

```
1ALTER TABLE events
2ATTACH PARTITION ID 'all'
3FROM events;
```
Copy command

```
10 rows in set. Elapsed: 3.892 sec.
20 rows in set. Elapsed: 7.957 sec.
30 rows in set. Elapsed: 15.894 sec.
40 rows in set. Elapsed: 33.655 sec.
50 rows in set. Elapsed: 68.870 sec.
```
Copy command
And now we’ve got a lot more records:

```
1SELECT count()
2FROM events;
```
Copy command

```
1┌───count()─┐
21. │ 390046368 │ -- 390.05 million
3   └───────────┘
```
Copy command
The following query returns the number of rows related to Lionel Messi:

```
1SELECT count()
2FROM events
3WHERE json.player.name = 'Lionel Andrés Messi Cuccittini'
4SETTINGS use_skip_indexes = 1;
```
Copy command
We can disable the text index by setting `use_skip_indexes = 0`. Running this query gives us the following result:

```
1┌─count()─┐
21. │ 4268960 │ -- 4.27 million
3   └─────────┘
```
Copy command
We’ll run it three times without the index:
