---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"ANN
topic: ann-vector-search-with-sql-powered-lsh-random-projections-clickhouse
ch_version_introduced: '0.933'
last_updated: '2026-09-07'
chunk_index: 12
total_chunks_in_doc: 23
---

2FROM glove_lsh 3LIMIT 1 4FORMAT Vertical 5 6Row 1: 7────── 8word: CountyFire 9vector: [-0.3471,-0.75666,...,-0.91871] 10bits: 6428829369849783588275113772152029372 <- how bit hash in binary ``` Copy command Once our index is created, we evaluate its size with the following query:

```
1SELECT name, formatReadableSize(sum(data_compressed_bytes)) AS compressed_size,
2	formatReadableSize(sum(data_uncompressed_bytes)) AS uncompressed_size,
3	round(sum(data_uncompressed_bytes) / sum(data_compressed_bytes), 2) AS ratio
4FROM system.columns
5WHERE table LIKE 'glove_lsh'
6GROUP BY name
7ORDER BY name DESC
8
9┌─name───┬─compressed_size─┬─uncompressed_size─┬─ratio─┐
10│ word   │ 8.37 MiB    	   │ 12.21 MiB     	   │  1.46 │
11│ vector │ 1.48 GiB    	   │ 1.60 GiB      	   │  1.08 │
12│ bits   │ 13.67 MiB   	   │ 21.75 MiB     	   │  1.59 │
13└────────┴─────────────────┴───────────────────┴───────┘
```
Copy command
Our `bits` column is over 100x smaller than our `vector`. The question is now whether this compressed representation of the space is faster to query...

### Querying \#

Before using our index, let's test a simple brute force search using cosine distance (as recommended for glove) to provide a benchmark of absolute quality and what we can consider the authoritative results. In the example below, we search for words similar to the word "dog":

```
1WITH
2   'dog' AS search_term,
3   (
4       SELECT vector
5       FROM glove
6       WHERE word = search_term
7       LIMIT 1
8   ) AS target_vector
9SELECT
10   word,
11   cosineDistance(vector, target_vector) AS score
12FROM glove
13WHERE lower(word) != lower(search_term)
14ORDER BY score ASC
15LIMIT 5
16
17
18┌─word────┬──────score─┐
19│ dogs    │ 0.11640698 │
20│ puppy   │ 0.14147866 │
21│ pet     │ 0.19425482 │
22│ cat     │ 0.19831467 │
23│ puppies │ 0.24826884 │
24└─────────┴────────────┘
25
2610 rows in set. Elapsed: 0.515 sec. Processed 2.21 million rows, 2.70 GB (2.87 million rows/s., 3.51 GB/s.)
27Peak memory usage: 386.24 MiB.
```
Copy command

> In the interest of simplicity, we use our glove index to look up the result. In a real search scenario, it is unlikely our word would be in the index and need to generate the vector embedding e.g. using an `embed` UDF function.

For the same query, we need to construct the bit hash above for our term using our index.For our hamming distance calculation between our target vector’s bit and those in each row, we use the [bitHammingDistance](https://clickhouse.com/docs/en/sql-reference/functions/bit-functions#bithammingdistance) function.
