---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"ClickHouse
topic: clickhouse-release-23-11-clickhouse
ch_version_introduced: '0.057'
last_updated: '2026-09-07'
chunk_index: 6
total_chunks_in_doc: 8
---

if the column contains N distinct values, N windows need to be created. In 23\.11, these partitions can effectively be constructed and evaluated in parallel. As an example, consider the following query, which uses the NOAA weather dataset.

```
1CREATE TABLE noaa
2(
3	`station_id` LowCardinality(String),
4	`date` Date32,
5	`tempAvg` Int32 COMMENT 'Average temperature (tenths of a degrees C)',
6	`tempMax` Int32 COMMENT 'Maximum temperature (tenths of degrees C)',
7	`tempMin` Int32 COMMENT 'Minimum temperature (tenths of degrees C)',
8	`precipitation` UInt32 COMMENT 'Precipitation (tenths of mm)',
9	`snowfall` UInt32 COMMENT 'Snowfall (mm)',
10	`snowDepth` UInt32 COMMENT 'Snow depth (mm)',
11	`percentDailySun` UInt8 COMMENT 'Daily percent of possible sunshine (percent)',
12	`averageWindSpeed` UInt32 COMMENT 'Average daily wind speed (tenths of meters per second)',
13	`maxWindSpeed` UInt32 COMMENT 'Peak gust wind speed (tenths of meters per second)',
14	`weatherType` Enum8('Normal' = 0, 'Fog' = 1, 'Heavy Fog' = 2, 'Thunder' = 3, 'Small Hail' = 4, 'Hail' = 5, 'Glaze' = 6, 'Dust/Ash' = 7, 'Smoke/Haze' = 8, 'Blowing/Drifting Snow' = 9, 'Tornado' = 10, 'High Winds' = 11, 'Blowing Spray' = 12, 'Mist' = 13, 'Drizzle' = 14, 'Freezing Drizzle' = 15, 'Rain' = 16, 'Freezing Rain' = 17, 'Snow' = 18, 'Unknown Precipitation' = 19, 'Ground Fog' = 21, 'Freezing Fog' = 22),
15	`location` Point,
16	`elevation` Float32,
17	`name` LowCardinality(String),
18	`country` LowCardinality(String)
19)
20ENGINE = MergeTree
21ORDER BY (country, date)
22
23
24INSERT INTO noaa SELECT *
25FROM s3('https://datasets-documentation.s3.eu-west-3.amazonaws.com/noaa/noaa_with_country.parquet')
```
Copy command
A simple window function might be used here to compute the moving average of temperature for every day and country. This requires us to **partition by country** (of which there are 214 in the dataset) and order by day. In computing the moving average we consider the last 5 datapoints.

```
1SELECT
2	country,
3	day,
4	max(tempAvg) AS temperature,
5	avg(temperature) OVER (PARTITION BY country ORDER BY day ASC ROWS BETWEEN 5 PRECEDING AND CURRENT ROW) AS moving_avg_temp
6FROM noaa
7WHERE country != ''
8GROUP BY
9	country,
10	date AS day
11ORDER BY
12	country ASC,
13	day ASC
```
Copy command
The intent of this simple function is best explained with a simple visualization.

![window_function.png](/_next/image?url=%2Fuploads%2Fwindow_function_be19f348ad.png&w=2048&q=75)
