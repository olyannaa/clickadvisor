---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"ClickHouse
topic: clickhouse-over-the-years-with-benchmarks-clickhouse
ch_version_introduced: '18.16'
last_updated: '2026-09-07'
chunk_index: 9
total_chunks_in_doc: 14
---

we need a bit more changes. We will create a schema without LowCardinality columns. And we have to add materialized columns with dates as we already supported best effort date time parsing, but there is no setting –date\_time\_input\_format\=best\_effort

```
1CREATE TABLE customer
2(
3        C_CUSTKEY       UInt32,
4        C_NAME          String,
5        C_ADDRESS       String,
6        C_CITY          String,
7        C_NATION        String,
8        C_REGION        String,
9        C_PHONE         String,
10        C_MKTSEGMENT    String
11)
12ENGINE = MergeTree ORDER BY (C_CUSTKEY);
13
14CREATE TABLE lineorder
15(
16    LO_ORDERKEY             UInt32,
17    LO_LINENUMBER           UInt8,
18    LO_CUSTKEY              UInt32,
19    LO_PARTKEY              UInt32,
20    LO_SUPPKEY              UInt32,
21    LO_ORDERDATE_RAW        String,
22    LO_ORDERPRIORITY        String,
23    LO_SHIPPRIORITY         UInt8,
24    LO_QUANTITY             UInt8,
25    LO_EXTENDEDPRICE        UInt32,
26    LO_ORDTOTALPRICE        UInt32,
27    LO_DISCOUNT             UInt8,
28    LO_REVENUE              UInt32,
29    LO_SUPPLYCOST           UInt32,
30    LO_TAX                  UInt8,
31    LO_COMMITDATE_RAW       String,
32    LO_SHIPMODE             String,
33    LO_ORDERDATE            Date MATERIALIZED parseDateTimeBestEffort(LO_ORDERDATE_RAW),
34    LO_COMMITDATE           Date MATERIALIZED parseDateTimeBestEffort(LO_COMMITDATE_RAW)
35)
36ENGINE = MergeTree PARTITION BY toYear(LO_ORDERDATE) ORDER BY (LO_ORDERDATE, LO_ORDERKEY);
37
38CREATE TABLE part
39(
40        P_PARTKEY       UInt32,
41        P_NAME          String,
42        P_MFGR          String,
43        P_CATEGORY      String,
44        P_BRAND         String,
45        P_COLOR         String,
46        P_TYPE          String,
47        P_SIZE          UInt8,
48        P_CONTAINER     String
49)
50ENGINE = MergeTree ORDER BY P_PARTKEY;
51
52CREATE TABLE supplier
53(
54        S_SUPPKEY       UInt32,
55        S_NAME          String,
56        S_ADDRESS       String,
57        S_CITY          String,
58        S_NATION        String,
59        S_REGION        String,
60        S_PHONE         String
61)
62ENGINE = MergeTree ORDER BY S_SUPPKEY;
```
Copy command
Format of tbl files is similar to CSV, but delimiter is \| and there is a delimiter in the end of the row that we need to remove. There are commas in the values, but no tabs, so we can update the data files and import them as TSV format. It is trivial just to use CSV or Template format in recent ClickHouse to import this data without converting, but in 2018 we had to improvise.
