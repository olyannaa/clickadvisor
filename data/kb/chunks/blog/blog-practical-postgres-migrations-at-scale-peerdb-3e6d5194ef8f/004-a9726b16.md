---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"Making
topic: making-large-postgres-migrations-practical-1tb-in-2-hours-with-peerdb-clickhouse
ch_version_introduced: '27.175'
last_updated: '2026-09-07'
chunk_index: 4
total_chunks_in_doc: 11
---

to database for restore 2pg_restore: processing data for table "public.firenibble" 3pg_restore: executing SEQUENCE SET firenibble_f0_seq 4 5real 1024m58.739s 6user 1133m6.474s 7sys 39m19.008s ``` Copy command The full table was loaded at the destination after **17 hours 5 minutes.**

#### Native logical replication \#

The source RDS was the publisher, with a publication created with just this one table.

```
1tb_test=> CREATE PUBLICATION fire_pub FOR TABLE firenibble;
2CREATE PUBLICATION
```
Copy command
From here, a subscription was created on Postgres managed by ClickHouse, pointing to the above database and publication.

```
1logical_replication_test=# CREATE SUBSCRIPTION rds_subscription
2logical_replication_test-# CONNECTION '<source_connection_string>'
3logical_replication_test-# PUBLICATION fire_pub;
4logical_replication_test=# NOTICE:  created replication slot "rds_subscription" on publisher
5logical_replication_test-# CREATE SUBSCRIPTION
```
Copy command
This immediately begins the initial load. Note that here too parallel loading of a single table is not available; it’s done by a single synchronization worker in Postgres.

```
1// grep Postgres subscriber logs for "synchronization"
22026-02-17 21:34:27.175 UTC [35026:1] (0,521/2): host=,db=,user=,app=,client= LOG: logical replication table synchronization worker for subscription "rds_subscription", table "firenibble" has started 
3
42026-02-18 06:15:18.842 UTC [35026:2] (0,521/8): host=,db=,user=,app=,client= LOG: logical replication table synchronization worker for subscription "rds_subscription", table "firenibble" has finished
```
Copy command
All in all, native logical replication loaded the data in **8 hours 40 minutes**.

#### PeerDB with 8 threads \#

PeerDB’s architecture involves **peers**, which point to data stores, and **mirrors**, which are data movement pipelines between peers. Here, the source peer was RDS and the target peer was Postgres managed by ClickHouse.

When creating a mirror in PeerDB, you can set a value for initial load parallelism as well as other parameters to determine size of logical partitions and **parallelism across tables**.

In our case, since we had 1 table, we had parallelism across tables set to 1, and per\-table parallelism to be 8\.

All in all, PeerDB synced the 1 TB table in **1 hour 49 minutes.**

![1.png](/_next/image?url=%2Fuploads%2F1_25ee8d61b6.png&w=2048&q=75)

#### Test matrix \#

We ran initial load tests across different sizes of the above table and parallelism specs of PeerDB. The results are below.
