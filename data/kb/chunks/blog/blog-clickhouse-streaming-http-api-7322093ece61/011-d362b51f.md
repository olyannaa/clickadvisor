---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"ClickHouse
topic: clickhouse-as-a-streaming-http-api-clickhouse
ch_version_introduced: '339.08'
last_updated: '2026-09-07'
chunk_index: 11
total_chunks_in_doc: 14
---

successful HTTP requests with the following query: ``` 1SYSTEM FLUSH LOGS; 2 3SELECT event_time, http_handler_name, http_request_url, query_duration_ms, read_rows, result_rows 4FROM system.query_log 5WHERE (type = 'QueryFinish') AND (http_request_url != '') 6ORDER BY event_time DESC 7LIMIT 3; ``` Copy command

```
1┌──────────event_time─┬─http_handler_name─┬─http_request_url───────────┬─query_duration_ms─┬─read_rows─┬─result_rows─┐
2│ 2026-09-02 12:28:58 │ town_sales        │ /api/town-sales            │                12 │   2300102 │          10 │
3│ 2026-09-02 12:28:16 │                   │ /default/uk_price_paid.csv │                26 │  26946287 │           3 │
4│ 2026-09-02 12:28:12 │ town_sales_path   │ /api/town-sales/LONDON     │                15 │    647168 │       13958 │
5└─────────────────────┴───────────────────┴────────────────────────────┴───────────────────┴───────────┴─────────────┘
```
Copy command
For a named handler, `http_handler_name` contains the handler that processed the request. It is empty for a direct table API request, but `http_request_url` still records the requested path, excluding any query string.

26\.8 also introduces a new system table, `system.user_query_log`, that lets users see their own query history without needing access to the full query log. We can therefore return all the API requests executed by `api_user` by running the following:

```
1./clickhouse client --user api_user <<'SQL'
2SELECT
3    event_time, http_handler_name, http_request_url,
4    query_duration_ms, read_rows, result_rows
5FROM system.user_query_log
6WHERE type = 'QueryFinish' AND http_request_url != ''
7ORDER BY event_time DESC
8LIMIT 3 FORMAT Pretty;
9SQL
```
Copy command
## Controlling access to handlers \#

A handler's URL can be requested by any user who can reach the HTTP server, but its query runs with the permissions of the authenticated user. There is no separate permission for invoking an individual handler.

To see this in action, let's create another API user who doesn't have access to `uk_price_paid`:

```
1CREATE USER restricted_api_user
2IDENTIFIED WITH no_password
3HOST LOCAL
4SETTINGS default_format = 'JSONEachRow', limit = 10;
```
Copy command
We can confirm its grants by running:

```
1SHOW GRANTS FOR restricted_api_user;
```
Copy command

```
1Ok.
2
30 rows in set. Elapsed: 0.002 sec.
```
Copy command
Now let's try to call the `expensive_areas` handler as this user:

```
1curl --silent --user 'restricted_api_user:' 'http://localhost:8123/api/expensive-areas'
```
Copy command
We'll see the following output:
