---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"ClickHouse
topic: clickhouse-cloud-fast-updatable-lookups-with-the-join-table-engine-clickhouse
ch_version_introduced: '26.8'
last_updated: '2026-09-07'
chunk_index: 4
total_chunks_in_doc: 6
---

Example: Data Enrichment \# Probably the most meaningful use case is enrichment / dimensional modeling with an `ANY LEFT` join. To illustrate this specific case, let's take the example from the ClickHouse [docs](https://clickhouse.com/docs/engines/table-engines/special/join#example) and modify it a bit:

```
1-- Create the fact table and insert some data
2CREATE OR REPLACE TABLE id_val (
3    `id` UInt32,
4    `val` UInt32
5) ENGINE = MergeTree
6ORDER BY (id);
7
8INSERT INTO id_val VALUES
9    (1, 11), (2, 12), (3, 13);
10
11-- Creating the right-side Join table:
12CREATE OR REPLACE TABLE id_val_join (
13    `id` UInt32,
14    `val` UInt8
15) ENGINE = Join(ANY, LEFT, id);
16
17-- Insert some values
18INSERT INTO id_val_join VALUES
19    (1, 21), (1, 22), (3, 23);
20
21-- Enrichment query
22SELECT *
23FROM id_val
24ANY LEFT JOIN id_val_join USING (id);
```
Copy command

```
1┌─id─┬─val─┬─id_val_join.val─┐
21. │  1 │  11 │              22 │
32. │  2 │  12 │               0 │
43. │  3 │  13 │              23 │
5   └────┴─────┴─────────────────┘
```
Copy command
Now, let's find out what happens in the `Join` table and in the underlying table when we upsert an entry for key `1`.

```
1-- And another insert
2INSERT INTO id_val_join VALUES (1,42);
```
Copy command
Look up the underlying table:

```
1SELECT database, name, uuid, engine
2FROM system.tables
3WHERE name = 'id_val_join'
4FORMAT Vertical;
```
Copy command

```
1database:                         default
2name:                             id_val_join
3uuid:                             64f169ee-977d-46c2-b067-580fdf8c1d4b
4engine:                           SharedJoin
```
Copy command
The `Join` table deduplicates and keeps the latest entry only:

```
1SELECT * FROM id_val_join;
```
Copy command

```
1┌─id─┬─val─┐
21. │  3 │  23 │
32. │  1 │  42 │
4   └────┴─────┘
```
Copy command
Constructing the underlying `ReplacingMergeTree` table from the UUID, we see this one retains the duplicates until they are merged out:

```
1SELECT * FROM default.`.inner_id.SharedJoin.64f169ee-977d-46c2-b067-580fdf8c1d4b`;
```
Copy command

```
1┌─id─┬─val─┐
21. │  1 │  22 │
32. │  3 │  23 │
43. │  1 │  42 │
5   └────┴─────┘
```
Copy command
Finally, running the enrichment query again, we see how the updated dimension entry reflects in the result:

```
1SELECT *
2FROM id_val
3ANY LEFT JOIN id_val_join USING (id);
```
Copy command
