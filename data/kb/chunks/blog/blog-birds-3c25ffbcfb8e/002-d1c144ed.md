---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"Birds
topic: birds-in-the-cloud-clickhouse
ch_version_introduced: '52.2278'
last_updated: '2026-09-07'
chunk_index: 2
total_chunks_in_doc: 6
---

./clickhouse install # or run it as ./clickhouse without installation ``` Copy command ### Let's preview the dataset structure \# ClickHouse allows the processing of files inside archives without decompressing. ``` 1DESCRIBE file('2023-eBird-dwca-1.0.zip :: *.csv'); ``` Copy command

```
1┌─name──────────────┬─type──────────────┐
2 1. │ basisofrecord     │ Nullable(String)  │
3 2. │ institutioncode   │ Nullable(String)  │
4 3. │ collectioncode    │ Nullable(String)  │
5 4. │ catalognumber     │ Nullable(String)  │
6 5. │ occurrenceid      │ Nullable(String)  │
7 6. │ recordedby        │ Nullable(String)  │
8 7. │ year              │ Nullable(Int64)   │
9 8. │ month             │ Nullable(Int64)   │
10 9. │ day               │ Nullable(Int64)   │
1110. │ publishingcountry │ Nullable(String)  │
1211. │ country           │ Nullable(String)  │
1312. │ stateprovince     │ Nullable(String)  │
1413. │ county            │ Nullable(String)  │
1514. │ decimallatitude   │ Nullable(Float64) │
1615. │ decimallongitude  │ Nullable(Float64) │
1716. │ locality          │ Nullable(String)  │
1817. │ kingdom           │ Nullable(String)  │
1918. │ phylum            │ Nullable(String)  │
2019. │ class             │ Nullable(String)  │
2120. │ order             │ Nullable(String)  │
2221. │ family            │ Nullable(String)  │
2322. │ genus             │ Nullable(String)  │
2423. │ specificepithet   │ Nullable(String)  │
2524. │ scientificname    │ Nullable(String)  │
2625. │ vernacularname    │ Nullable(String)  │
2726. │ taxonremarks      │ Nullable(String)  │
2827. │ taxonconceptid    │ Nullable(String)  │
2928. │ individualcount   │ Nullable(Int64)   │
30    └───────────────────┴───────────────────┘
```
Copy command
The `DESCRIBE` query gives an automatically inferred schema for the data. The `file` table function allows the processing of files on the local filesystem and the `::` notation allows the processing of files inside archives. All filenames support glob\-expansions with `*`, `?`, `**`, `{0..9}`, `{abc,def}`. For CSV files, ClickHouse automatically detects if there is a header and uses the header if it is present. It also automatically detects data types of columns. With all these conveniences, ClickHouse makes data processing seamless.

### Preview the data \#

```
1SELECT * FROM file('2023-eBird-dwca-1.0.zip :: *.csv') LIMIT 1;
```
Copy command
