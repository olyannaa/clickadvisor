---
source: blog
url: https://clickhouse.com/blog/full-text-search-ga-release
topic: do-you-still-need-elasticsearch-for-log-analytics-clickhouse-says-no
ch_version_introduced: '120.3'
last_updated: '2026-06-12'
chunk_index: 6
total_chunks_in_doc: 17
---

they are part of the engines’ normal execution path. ### Measuring cold runs [\#](/blog/elasticsearch-log-analytics-clickhouse#measuring-cold-runs) For a cold run, we fully shut down both systems, dropped the Linux page cache, restarted the servers, and only then executed a query.

This means:

- no data pages were cached in memory
- the engine had to load required data from disk
- runtime reflects storage efficiency, pruning ability, and raw execution speed

Each query in the OTel log query suite was executed under these cold\-start conditions, and the reported cold totals are the sum of all per\-query runtimes.

### Measuring hot runs [\#](/blog/elasticsearch-log-analytics-clickhouse#measuring-hot-runs)

Hot runs kept both systems running between executions without dropping the Linux page cache.

For each query:

- the same query was executed **three times in a row**
- full query\-result caches remained disabled
- filter\-evaluation caches remained enabled
- the **fastest** of the three runs was recorded as the hot result

This isolates repeated\-query performance without allowing full result reuse.

Each query in the OTel log query suite was measured under these conditions, and the reported hot totals are the sum of all per\-query runtimes.

### Elasticsearch Query DSL vs ES\|QL [\#](/blog/elasticsearch-log-analytics-clickhouse#elasticsearch-query-dsl-vs-esql)

Elasticsearch results shown in the main charts use [Query DSL](https://www.elastic.co/docs/explore-analyze/query-filter/languages/querydsl). We also implemented the full suite in [ES\|QL](https://www.elastic.co/docs/explore-analyze/query-filter/languages/esql-kibana), but observed consistently slower runtimes in our tests, so Query DSL serves as the stronger Elasticsearch baseline throughout the main benchmark.

The public benchmark repository [includes](https://github.com/ClickHouse/TextBench/tree/main/elasticsearch/results/es_dsl_vs_esql) the corresponding cold and hot per\-query comparison charts for Query DSL vs ES\|QL.

With the benchmark methodology established, we next examine how both systems store OTel logs on disk. That on\-disk layout is essential context for the results that follow.

### What about OpenSearch? [\#](/blog/elasticsearch-log-analytics-clickhouse#what-about-opensearch)

This benchmark focuses on Elasticsearch, not OpenSearch. We chose Elasticsearch as the baseline because it remains the best\-known reference point for full\-text log search. OpenSearch is closely related and often chosen by teams looking for an Elasticsearch\-compatible, AWS\-native, or lower\-cost option.
