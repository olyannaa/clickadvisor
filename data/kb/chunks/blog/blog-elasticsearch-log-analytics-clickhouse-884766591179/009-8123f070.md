---
source: blog
url: https://clickhouse.com/blog/full-text-search-ga-release
topic: do-you-still-need-elasticsearch-for-log-analytics-clickhouse-says-no
ch_version_introduced: '120.3'
last_updated: '2026-06-12'
chunk_index: 9
total_chunks_in_doc: 17
---

primary index in this comparison. ### Points and norms for filtering and scoring [\#](/blog/elasticsearch-log-analytics-clickhouse#points-and-norms-for-filtering-and-scoring) In Elasticsearch, **④ [points](https://www.elastic.co/fr/blog/lucene-points-6-0)** support efficient filtering and range queries on numeric and date fields, while [norms](https://www.elastic.co/docs/reference/elasticsearch/mapping-reference/norms) store per\-document scoring factors for text relevance.

ClickHouse does not use a separate points structure: numeric and date filtering is handled through columnar scans plus sparse primary and [skipping indexes](https://clickhouse.com/docs/optimize/skipping-indexes). ClickHouse also does not currently score full\-text search results, which is not relevant for our logging use case.

Now that the storage building blocks are clear, we can turn to their combined effect on disk usage.

## OTel log storage footprint [\#](/blog/elasticsearch-log-analytics-clickhouse#otel-log-storage-footprint)

The storage structures above explain where bytes go. To compare overall footprint fairly, we first aligned the major variables that most affect disk usage:

1. **Same on\-disk order**: Data is sorted on disk by the same keys — (ServiceName, Timestamp) in ClickHouse via the sorting key, and in Elasticsearch via [index sorting](https://www.elastic.co/docs/reference/elasticsearch/index-settings/sorting) on the same fields.
2. **Same compression algorithm**: The main structures that store original log content use the same compression algorithm. ZSTD for ClickHouse column files and ZSTD for Elasticsearch stored fields.
3. **Same full\-text inverted\-index scope**: Both systems build a full\-text inverted index only on the Body field. In Elasticsearch, all other string fields are mapped as keyword, meaning they support exact\-match filtering but are not analyzed for full\-text search.

Finally, as mentioned [earlier](/blog/elasticsearch-log-analytics-clickhouse#shard-layout), the comparison uses a storage\-optimized single\-node setup: no replicas in either system, one shard in ClickHouse, and multiple shards in Elasticsearch, following best practices for optimal shard size (50GB per shard), and force\-merged to one segment per shard.

With that in mind, the chart below shows the OTel log storage footprint in both systems for 1B, 10B, and 50B stored OTel log records.

![Blog-FTS-vs-Elasticsearch.002.png](/uploads/Blog_FTS_vs_Elasticsearch_002_e35ca8bcac.png)
To make the totals easier to interpret, the chart breaks them down into the individual on\-disk data structures introduced in the [previous section](/blog/elasticsearch-log-analytics-clickhouse#how-otel-logs-are-stored-on-disk). The legend maps each colored segment to its corresponding structure in Elasticsearch and ClickHouse, while the underlying sizes come from Elasticsearch’s [\_disk\_usage API](https://github.com/ClickHouse/TextBench/blob/main/elasticsearch/disk_usage.sh) and ClickHouse’s [parts system table](https://github.com/ClickHouse/TextBench/blob/main/clickhouse/_table-size.sql).

Note: The ClickHouse sparse primary index is included in the totals, but not visible at this scale ([6\.47 MiB](https://github.com/ClickHouse/TextBench/blob/main/clickhouse/results/storage/es_vs_ch/clickhouse_primary_index_sizes.jsonl) / [64\.26 MiB](https://github.com/ClickHouse/TextBench/blob/main/clickhouse/results/storage/es_vs_ch/clickhouse_primary_index_sizes.jsonl) / [320\.56 MiB](https://github.com/ClickHouse/TextBench/blob/main/clickhouse/results/storage/es_vs_ch/clickhouse_primary_index_sizes.jsonl)).

### Overall storage footprint [\#](/blog/elasticsearch-log-analytics-clickhouse#overall-storage-footprint)
