---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"ClickHouse
topic: clickhouse-vs-bigquery-using-clickhouse-to-serve-real-time-queries-on-top-of-bigquery-data-clickhouse
ch_version_introduced: '22.712'
last_updated: '2026-09-07'
chunk_index: 9
total_chunks_in_doc: 21
---

at a service or user level. The `Access key` and `Secret` can be substituted for the `aws_access_key_id`, `aws_secret_access_key`, respectively. In the secure example below we query the data in place \- a typical access pattern for adhoc analysis:

```
1SELECT max(number) AS max_block_number
2FROM s3('https://storage.googleapis.com/clickhouse_public_datasets/ethereum/blocks/*.csv.gz', 'CSVWithNames')
3
4┌─max_block_number─┐
5│       16542640 │
6└──────────────────┘
7
81 row in set. Elapsed: 15.926 sec. Processed 16.54 million rows, 148.88 MB (1.04 million rows/s., 9.35 MB/s.)
```
Copy command
The above use of CSV is suboptimal as an exchange format. [Parquet](https://clickhouse.com/docs/en/sql-reference/formats/#data-format-parquet), as a column\-oriented format, represents a better interchange format since it is inherently compressed and faster for BigQuery to export and ClickHouse to query. If using Parquet, you will need to map Nullable columns to non\-Null equivalents due to the above schema choices (or use Nullable columns in ClickHouse). For example, below we use the [`ifNull`](https://clickhouse.com/docs/en/sql-reference/functions/functions-for-nulls/#ifnull) function to map null values of `base_fee_per_gas` to 0\. Additional compression algorithms [available, when exporting to Parquet](https://cloud.google.com/bigquery/docs/exporting-data#data-locations) may improve the process further, but we will leave this exercise to the reader.

```
1INSERT INTO blocks
2SELECT number, hash, parent_hash, nonce, sha3_uncles, logs_bloom, transactions_root, state_root, receipts_root, miner, difficulty, total_difficulty, size, extra_data, gas_limit, gas_used, timestamp, transaction_count, ifNull(base_fee_per_gas, 0) AS base_fee_per_gas
3FROM s3Cluster('default', 'https://storage.googleapis.com/bigquery_ethereum_export/blocks/*.parquet', 'GOOGR2DYAAX6RVODIMREAVPB', '+tNQdQQ0DCEItWQlJseXcidKSG6pOU65o1r0N17O')
```
Copy command
We repeated the above exercise for each of our tables, recording the timings below using Parquet format. Using this method, we were able to transfer 4TB from BigQuery to ClickHouse in less than an hour!
