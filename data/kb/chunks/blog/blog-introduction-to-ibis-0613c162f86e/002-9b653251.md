---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"An
topic: an-introduction-to-ibis-clickhouse
ch_version_introduced: '24.7'
last_updated: '2026-09-07'
chunk_index: 2
total_chunks_in_doc: 7
---

4 overwrite=True 5) ``` Copy command This command imports the dataset into a table called `flights` and replaces the table if it already exists. In another tab, let’s connect to ClickHouse to see what this command has done:

```
1./clickhouse client -m
2
3ClickHouse client version 24.7.1.2215 (official build).
4Connecting to localhost:9000 as user default.
5Connected to ClickHouse server version 24.7.1.
```
Copy command
Once it’s connected, we can get a list of the tables:

```
1SHOW TABLES
2
3   ┌─name────┐
41. │ flights │
5   └─────────┘
6
71 row in set. Elapsed: 0.002 sec.
```
Copy command
## Exploring the Ibis flights dataset \#

Let’s have a look at what fields we’ve got in that `flights` table:

```
1DESCRIBE TABLE flights
2SETTINGS describe_compact_output = 1
3
4Query id: 7d497dee-ea8d-4b07-8b32-3f32f775ca32
5
6    ┌─name───────────┬─type────────────────────┐
7 1. │ year           │ Nullable(Int64)         │
8 2. │ month          │ Nullable(Int64)         │
9 3. │ day            │ Nullable(Int64)         │
10 4. │ dep_time       │ Nullable(String)        │
11 5. │ sched_dep_time │ Nullable(Int64)         │
12 6. │ dep_delay      │ Nullable(String)        │
13 7. │ arr_time       │ Nullable(String)        │
14 8. │ sched_arr_time │ Nullable(Int64)         │
15 9. │ arr_delay      │ Nullable(String)        │
1610. │ carrier        │ Nullable(String)        │
1711. │ flight         │ Nullable(Int64)         │
1812. │ tailnum        │ Nullable(String)        │
1913. │ origin         │ Nullable(String)        │
2014. │ dest           │ Nullable(String)        │
2115. │ air_time       │ Nullable(String)        │
2216. │ distance       │ Nullable(Int64)         │
2317. │ hour           │ Nullable(Int64)         │
2418. │ minute         │ Nullable(Int64)         │
2519. │ time_hour      │ Nullable(DateTime64(6)) │
26    └────────────────┴─────────────────────────┘
```
Copy command
So far, so good. Let’s return to our Python REPL and explore the `flight` data more thoroughly. First, we’ll create a reference to the table:

```
1flights = con.table("flights")
2flights.schema()
```
Copy command

```
1ibis.Schema {
2  year            int64
3  month           int64
4  day             int64
5  dep_time        string
6  sched_dep_time  int64
7  dep_delay       int64
8  arr_time        string
9  sched_arr_time  int64
10  arr_delay       int64
11  carrier         string
12  flight          int64
13  tailnum         string
14  origin          string
15  dest            string
16  air_time        string
17  distance        int64
18  hour            int64
19  minute          int64
20  time_hour       timestamp(6)
21}
```
Copy command
And now, let’s have a look at one row of the table:

```
1flights.head(n=1).to_pandas().T
```
Copy command
