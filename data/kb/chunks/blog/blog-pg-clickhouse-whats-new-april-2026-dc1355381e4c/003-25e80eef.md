---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"
topic: what-s-new-in-pg-clickhouse-jsonb-support-sql-value-functions-streaming-and-more-clickhouse
ch_version_introduced: '35.342'
last_updated: '2026-09-07'
chunk_index: 3
total_chunks_in_doc: 9
---

1QUERY PLAN 2---------------------------------------------------------------------------------------------------------- 3 Foreign Scan on customer.events 4 Output: id, event, props 5 Remote SQL: SELECT id, event, props FROM "default".events WHERE ((toJSONString(props.cid) = '"C300"')) 6(3 rows) ``` Copy command Executing the query returns the expected result:

```
1SELECT id, event, props
2FROM customer.events 
3WHERE props -> 'cid' = '"C300"'::jsonb;
```
Copy command

```
1id | event |                                 props
2----+-------+------------------------------------------------------------------------
3  4 | order | {"cid": "C300", "address": {"city": "New York, USA", "code": "10030"}}
4(1 row)
```
Copy command
The same pattern applies to the [JSONB functions](https://www.postgresql.org/docs/18/functions-json.html "Postgres Docs: JSON Functions and Operators") `jsonb_extract_path()` and
`jsonb_extract_path_text()` functions, which also allowing multiple paths to
get to nested values, as visible in the `Remote SQL` for this plan:

```
1EXPLAIN (VERBOSE, COSTS OFF)
2SELECT id, event, props FROM customer.events
3WHERE jsonb_extract_path_text(props, 'address', 'city') = 'Paris, France';
```
Copy command

```
1QUERY PLAN
2------------------------------------------------------------------------------------------------------------
3 Foreign Scan on customer.events
4   Output: id, event, props
5   Remote SQL: SELECT id, event, props FROM "default".events WHERE ((props.address.city = 'Paris, France'))
6(3 rows)
```
Copy command
Of course execution returns the expected results:

```
1SELECT id, event, props 
2FROM customer.events
3WHERE jsonb_extract_path_text(props, 'address', 'city') = 'Paris, France';
```
Copy command

```
1id | event |                                 props
2----+-------+------------------------------------------------------------------------
3  1 | order | {"cid": "C100", "address": {"city": "Paris, France", "code": "75001"}}
4(1 row)
```
Copy command
And the same goes for pushing down comparisons to JSON values using
`jsonb_extract_path()`:

```
1EXPLAIN (VERBOSE, COSTS OFF)
2SELECT id, event, props 
3FROM customer.events
4WHERE jsonb_extract_path(props, 'address', 'city') = '"New York, USA"';
```
Copy command

```
1QUERY PLAN
2----------------------------------------------------------------------------------------------------------------------------
3 Foreign Scan on customer.events
4   Output: id, event, props
5   Remote SQL: SELECT id, event, props FROM "default".events WHERE ((toJSONString(props.address.city) = '"New York, USA"'))
6(3 rows)
```
Copy command

```
1SELECT id, event, props 
2FROM customer.events
3WHERE jsonb_extract_path(props, 'address', 'city') = '"New York, USA"';
```
Copy command

```
1id | event |                                 props
2----+-------+------------------------------------------------------------------------
3  4 | order | {"cid": "C300", "address": {"city": "New York, USA", "code": "10030"}}
4(1 row)
```
Copy command
## SQL value functions \#
