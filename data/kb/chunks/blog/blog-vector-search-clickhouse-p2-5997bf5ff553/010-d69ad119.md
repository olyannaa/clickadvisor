---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"Vector
topic: vector-search-with-clickhouse-part-2-clickhouse
ch_version_introduced: '0.33110910654067993'
last_updated: '2026-09-07'
chunk_index: 10
total_chunks_in_doc: 29
---

in an embedding is random. For this reason, we recommend the ZSTD codec for compressing embeddings. Below we show the compression ratio for our vector columns in four tables of increasing size: 1m, 10m, 100m, and 2b rows.

```
1SELECT
2	table,
3	name,
4	formatReadableSize(sum(data_compressed_bytes)) AS compressed_size,
5	formatReadableSize(sum(data_uncompressed_bytes)) AS uncompressed_size,
6	round(sum(data_uncompressed_bytes) / sum(data_compressed_bytes), 2) AS ratio
7FROM system.columns
8WHERE (table IN ('laion_100m', 'laion_1m', 'laion_10m', 'laion_2b')) AND (name IN ('text_embedding', 'image_embedding'))
9GROUP BY
10	table,
11	name
12ORDER BY table DESC
13
14┌─table──────┬─name────────────┬─compressed_size─┬─uncompressed_size─┬─ratio─┐
15│ laion_1m   │ text_embedding  │ 1.60 GiB    	│ 2.50 GiB      	 │  1.56 │
16│ laion_1m   │ image_embedding │ 1.61 GiB    	│ 2.50 GiB      	 │  1.55 │
17│ laion_10m  │ text_embedding  │ 18.36 GiB   	│ 28.59 GiB     	 │  1.56 │
18│ laion_10m  │ image_embedding │ 18.36 GiB   	│ 28.59 GiB     	 │  1.56 │
19│ laion_100m │ text_embedding  │ 181.64 GiB  	│ 286.43 GiB    	 │  1.58 │
20│ laion_100m │ image_embedding │ 182.29 GiB  	│ 286.43 GiB    	 │  1.57 │
21│ laion_1b   │ image_embedding │ 1.81 TiB    	│ 2.81 TiB      	 │  1.55 │
22│ laion_1b   │ text_embedding  │ 1.81 TiB    	│ 2.81 TiB      	 │  1.55 │
23└────────────┴─────────────────┴─────────────────┴───────────────────┴───────┘
24
256 rows in set. Elapsed: 0.006 sec.
```
Copy command
While compression rates can [usually be influenced by primary key selection](https://clickhouse.com/docs/en/optimize/sparse-primary-indexes), this constant compression ratio of 1\.56 is unlikely to be impacted by how the data is sorted. The compression level of the ZSTD codec can be increased from its default value of 1 in ClickHouse Cloud. This delivers around a 10% improvement, compressing our data by 1\.71 on a 10 million row sample:

```
1SELECT
2	table,
3	name,
4	formatReadableSize(sum(data_compressed_bytes)) AS compressed_size,
5	formatReadableSize(sum(data_uncompressed_bytes)) AS uncompressed_size,
6	round(sum(data_uncompressed_bytes) / sum(data_compressed_bytes), 2) AS ratio
7FROM system.columns
8WHERE (table IN ('laion_10m_zstd_3')) AND (name IN ('text_embedding', 'image_embedding'))
9GROUP BY
10	table,
11	name
12ORDER BY table DESC
13
14┌─table────────────┬─name────────────┬─compressed_size─┬─uncompressed_size─┬─ratio─┐
15│ laion_10m_zstd_3 │ text_embedding  │ 16.68 GiB   	   │ 28.56 GiB     	   │  1.71 │
16│ laion_10m_zstd_3 │ image_embedding │ 16.72 GiB   	   │ 28.56 GiB     	   │  1.71 │
17└──────────────────┴─────────────────┴─────────────────┴───────────────────┴───────┘
18
192 rows in set. Elapsed: 0.026 sec.
```
Copy command
Note that higher values for ZSTD will slow down compression and data insertion, although decompression speeds should remain reasonably constant (around [20% variance](https://engineering.fb.com/2016/08/31/core-data/smaller-and-faster-data-compression-with-zstandard/))).
