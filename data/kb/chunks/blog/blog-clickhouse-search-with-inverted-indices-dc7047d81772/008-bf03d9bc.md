---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"Introducing
topic: introducing-inverted-indices-in-clickhouse-clickhouse
ch_version_introduced: '0.006'
last_updated: '2026-09-07'
chunk_index: 8
total_chunks_in_doc: 10
---

read at a time. ## A Real\-world Example \# Finally, it is time to demonstrate the inverted index. We use the Hacker News dataset, which contains 28\.7 million comments posted on [Hacker News](https://news.ycombinator.com/). Let's first import the data:

```
1CREATE TABLE hackernews
2(
3    `id` UInt64,
4    `deleted` UInt8,
5    `type` String,
6    `author` String,
7    `timestamp` DateTime,
8    `comment` String,
9    `dead` UInt8,
10    `parent` UInt64,
11    `poll` UInt64,
12    `children` Array(UInt32),
13    `url` String,
14    `score` UInt32,
15    `title` String,
16    `parts` Array(UInt32),
17    `descendants` UInt32
18)
19ENGINE = MergeTree
20ORDER BY (type, author);
21
22INSERT INTO hackernews
23    SELECT * FROM s3(
24        'https://datasets-documentation.s3.eu-west-3.amazonaws.com/hackernews/hacknernews.parquet',
25        'Parquet',
26        'id UInt64,
27         deleted UInt8,
28         type String,
29         by String,
30         time DateTime,
31         text String,
32         dead UInt8,
33         parent UInt64,
34         poll UInt64,
35         kids Array(UInt32),
36         url String,
37         score UInt32,
38         title String,
39         parts Array(UInt32),
40         descendants UInt32');
```
Copy command
To find out how many comments mention "ClickHouse", we can run:

```
1SELECT count(*)
2FROM hackernews
3WHERE hasToken(lower(comment), 'ClickHouse');
4
5┌─count()─┐
6│     516 │
7└─────────┘
8
91 row in set. Elapsed: 0.843 sec. Processed 28.74 million rows, 9.75 GB (34.08 million rows/s., 11.57 GB/s.)
```
Copy command
On my machine, the query took 0\.843 seconds to complete. Let's now create an inverted index on the "comment" column. Note that we index lowercase comments to find terms unrelated to their case.

```
1ALTER TABLE hackernews ADD INDEX comment_idx(lower(comment)) TYPE inverted;
2ALTER TABLE hackernews MATERIALIZE INDEX comment_idx;
```
Copy command
Materialization of the index takes a while (to check if the index was created, use the system table `system.data_skipping_indices`). Let's run the query again:
