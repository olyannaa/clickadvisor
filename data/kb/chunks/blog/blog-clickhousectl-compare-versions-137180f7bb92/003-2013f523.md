---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"Comparing
topic: comparing-clickhouse-versions-with-clickhousectl-clickhouse
ch_version_introduced: '0.1'
last_updated: '2026-09-07'
chunk_index: 3
total_chunks_in_doc: 8
---

9000 9 Version: 25.12.10.7 ``` Copy command This server uses the default HTTP and TCP ports. We can start a server running ClickHouse 26\.3 as well: ``` 1chctl local server start --version 26.3 --name new ``` Copy command

```
1Resolving 26.3...
2Note: 1 server already running (use `clickhousectl local server list` to see them)
3Note: default ports in use, auto-assigned HTTP:8124 TCP:9001
4Server 'new' started in background (PID: 58406)
5  HTTP port: 8124
6  TCP port:  9001
7  Version:   26.3.10.30
```
Copy command
This time it recognizes that there's already a server running and therefore uses different ports.

We can run the following command to check which servers are running:

```
1chctl local server list
```
Copy command

```
1╭──────┬─────────┬───────┬────────────┬───────────┬──────────╮
2│ Name │ Status  │ PID   │ Version    │ HTTP Port │ TCP Port │
3├──────┼─────────┼───────┼────────────┼───────────┼──────────┤
4│ new  │ running │ 58406 │ 26.3.10.30 │ 8124      │ 9001     │
5│ old  │ running │ 58145 │ 25.12.10.7 │ 8123      │ 9000     │
6╰──────┴─────────┴───────┴────────────┴───────────┴──────────╯
```
Copy command
This shows the servers running for a given project, which means if we run the command from a different directory, we'll see the following output:

```
1No servers
```
Copy command
We can use the `--global` flag to show all servers running across our machine:

```
1chctl local server list --global
```
Copy command

```
1╭──────┬─────────┬───────┬────────────┬───────────┬──────────┬──────────────╮
2│ Name │ Status  │ PID   │ Version    │ HTTP Port │ TCP Port │ Project      │
3├──────┼─────────┼───────┼────────────┼───────────┼──────────┼──────────────┤
4│ old  │ running │ 58145 │ 25.12.10.7 │ 8123      │ 9000     │ .../ch-test  │
5│ new  │ running │ 58406 │ 26.3.10.30 │ 8124      │ 9001     │ .../ch-test  │
6╰──────┴─────────┴───────┴────────────┴───────────┴──────────┴──────────────╯
```
Copy command
## Loading data into ClickHouse \#

Now that we've got our servers running, it's time to load some data.
We'll first connect to the 25\.12 server:

```
1chctl local client --name old -mn
```
Copy command
And then run the following query to create the [UK price paid](https://clickhouse.com/docs/getting-started/example-datasets/uk-price-paid) table:
