---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"Agentic
topic: agentic-bi-in-slack-mcp-powered-analytics-and-visualisations-clickhouse
ch_version_introduced: '26.8'
last_updated: '2026-09-07'
chunk_index: 3
total_chunks_in_doc: 7
---

100}, {"category": "B", "value": 200}]}, 17 "mark": "bar", 18 "encoding": { 19 "x": {"field": "category", "type": "nominal"}, 20 "y": {"field": "value", "type": "quantitative"} 21 } 22} ``` Copy command And to turn a returned spec into a visualisation:

```
1async def render_and_upload_chart(client, channel, thread_ts, vega_lite_spec, title="Chart"):
2    """Render Vega-Lite spec to PNG and upload to Slack"""
3    try:
4        # Parse the Vega-Lite specification
5        if isinstance(vega_lite_spec, str):
6            spec = json.loads(vega_lite_spec)
7        else:
8            spec = vega_lite_spec
9            
10        # Render to PNG using vl-convert
11        png_data = vlc.vegalite_to_png(spec)
12        
13        # Create temporary file
14        with tempfile.NamedTemporaryFile(suffix='.png', delete=False) as tmp_file:
15            tmp_file.write(png_data)
16            tmp_file.flush()
17            
18            # Upload file to Slack
19            response = await client.files_upload_v2(
20                channel=channel,
21                file=tmp_file.name,
22                title=title,
23                thread_ts=thread_ts
24            )
25            
26        # Clean up temp file
27        os.unlink(tmp_file.name)
28        return response
29        
30    except Exception as e:
31        logging.error(f"Error rendering and uploading chart: {e}")
32        return None
```
Copy command
No visualization servers. No complex rendering pipeline. Just using the LLM's understanding of both the data and Vega\-Lite to create appropriate charts on demand.

## What this looks like in practice \#

Here's a typical interaction:

**User**:

"What are the top 10 most downloaded python packages on pypi? create a chart"

**Bot**:

"**Key Findings:**

The top 10 most downloaded Python packages on PyPI are:1\. **boto3** \- 43\.1 billion downloads (AWS SDK)

2. **urllib3** \- 25\.6 billion downloads (HTTP client library)
3. **botocore** \- 23\.8 billion downloads (AWS core library)
4. **requests** \- 21\.7 billion downloads (HTTP library)
5. **setuptools** \- 20\.5 billion downloads (packaging tool)
6. **certifi** \- 19\.6 billion downloads (SSL certificates)
7. **idna** \- 19\.1 billion downloads (domain name handling)
8. **python\-dateutil** \- 17\.2 billion downloads (date/time utilities)
9. **typing\-extensions** \- 16\.8 billion downloads (type hints backport)
10. **s3transfer** \- 16\.7 billion downloads (AWS S3 transfer utility)"

![tmpd4gjyf5h.png](/_next/image?url=%2Fuploads%2Ftmpd4gjyf5h_453fbee1c5.png&w=2048&q=75)

The chart appears inline in the Slack thread. No new tabs, no context switching. The visualization and interpretation are right where the question was asked.

![mcp_slackbot_image_generation.gif](/_next/image?url=%2Fuploads%2Fmcp_slackbot_image_generation_e60f7dae94.gif&w=2048&q=75)

## Prompting strategy for charts \#

When a user asks a question, we prompt the model with:

- [The pre\-set system prompt](https://github.com/sdairs/examples/blob/b1116b92788f5f5f17ecd03046392ee70d390aab/ai/mcp/slackbot/main.py#L53)
- The natural‑language question from the user

The system prompt contains instructions for how the LLM should behave, and what it should produce. Including:
