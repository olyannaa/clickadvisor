---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"Agentic
topic: agentic-bi-in-slack-mcp-powered-analytics-and-visualisations-clickhouse
ch_version_introduced: '26.8'
last_updated: '2026-09-07'
chunk_index: 2
total_chunks_in_doc: 7
---

no ETL hops, just fast results. - **See the answer, trust the path.** You get both a visualization and the exact SQL, so if the analysis is valuable, you can turn it into a KPI or dashboard later.

We’re not replacing BI; we’re **right‑sizing** it. Dashboards remain for curated KPIs. Chat picks up the fluid, investigative 80%.

## What’s new since the last post \#

[In the first version of our Slack BI agent, the agent responded with a table or a short textual summary](https://clickhouse.com/blog/agentic-analytics-slack-clickhouse-mcp). In this iteration, we added **visualization capability**:

1. The LLM generates a **vega\-lite** visualisation spec (JSON)
2. The Python bot renders the chart from the spec using **vega\-altair** and attaches an image to the Slack message

This small addition changes the feel entirely \- threads become lightweight, *shareable* mini‑reports.

## A straightforward solution \#

We added visualization in about [50 lines of code](https://github.com/ClickHouse/examples/pull/311/files#diff-c2732de3eb904c5f74440667747fb319d949309b4966924ecc6a345fcf2471ec). The approach is simple:

1. The system prompt instructs the LLM to generate a vega\-lite compatible spec if a visualisation was requested by the user
2. If a spec is present in the LLMs output, create the visualisation with vega\-altair
3. Render the chart as a PNG and upload the image to the Slack thread

The system prompt now looks like:

```
1Available capabilities:
21) ClickHouse tools to explore databases, tables, and execute SQL queries
32) Chart generation by providing Vega-Lite specifications
4
5When users ask for data analysis with visualizations:
61. First query the database using available tools
72. If a visualization would be helpful, create a Vega-Lite chart specification
83. Format your Vega-Lite spec as JSON within ```json blocks
94. Choose appropriate chart types: bar charts for categories, line charts for time series, scatter for correlations, pie for proportions
10
11Example Vega-Lite specification format:
12```json
13{
14  "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
15  "title": "Chart Title",
16  "data": {"values": [{"category": "A", "value": 100}, {"category": "B", "value": 200}]},
17  "mark": "bar",
18  "encoding": {
19    "x": {"field": "category", "type": "nominal"},
20    "y": {"field": "value", "type": "quantitative"}
21  }
22}
```
Copy command
And to turn a returned spec into a visualisation:
