---
source: blog
url: https://schema.org","@type":"BlogPosting","headline":"ClickHouse
topic: clickhouse-release-25-8-clickhouse
ch_version_introduced: '25.8'
last_updated: '2026-09-07'
chunk_index: 6
total_chunks_in_doc: 11
---

files: ``` 1INSERT INTO taxis 2SELECT * 3FROM file('*.parquet'); ``` Copy command We can then query the table as shown below: ``` 1SELECT DOLocationID, _path, avg(fare_amount) 2FROM taxis 3WHERE PULocationID = 199 4GROUP BY ALL; ``` Copy command

```
1┌─DOLocationID─┬─_path───────────────────────────────────────────────────────────────┬─avg(fare_amount)─┐
2│           79 │ taxis/PULocationID=199/DOLocationID=79/7368231474740330498.parquet  │             45.7 │
3│          163 │ taxis/PULocationID=199/DOLocationID=163/7368231447649320960.parquet │               52 │
4│          229 │ taxis/PULocationID=199/DOLocationID=229/7368231455001935873.parquet │             37.3 │
5│          233 │ taxis/PULocationID=199/DOLocationID=233/7368231484399812609.parquet │             49.2 │
6│           41 │ taxis/PULocationID=199/DOLocationID=41/7368231474941657088.parquet  │             31.7 │
7│          129 │ taxis/PULocationID=199/DOLocationID=129/7368231437356498946.parquet │               10 │
8│           70 │ taxis/PULocationID=199/DOLocationID=70/7368231504532471809.parquet  │                3 │
9│          186 │ taxis/PULocationID=199/DOLocationID=186/7368231493912494080.parquet │             44.3 │
10└──────────────┴─────────────────────────────────────────────────────────────────────┴──────────────────┘
```
Copy command
## Data Lake improvements \#

### Contributed by Konstantin Vedernikov \#

ClickHouse’s Data Lake support also received improvements. Using the IcebergS3 table engine, you can now create an Apache Iceberg table, insert data, delete data, update data, and adjust the schema.

Let’s start by creating a table using the `IcebergLocal` table engine:

```
1CREATE TABLE demo (c1 Int32) 
2ENGINE = IcebergLocal('/Users/markhneedham/Downloads/ice/');
```
Copy command
Support for writing to Iceberg is still experimental, so we’ll need to set the following config:

```
1SET allow_experimental_insert_into_iceberg=1;
```
Copy command
Insert some data:

```
1INSERT INTO demo VALUES (1), (2), (3);
```
Copy command
Query that table:

```
1SELECT * 
2FROM demo;
```
Copy command
As expected, we have three rows:

```
1┌─c1─┐
2│  1 │
3│  2 │
4│  3 │
5└────┘
```
Copy command
Next, let’s delete the row where `c1=3`:

```
1DELETE FROM demo WHERE c1=3;
```
Copy command
And we’ll query the table again:

```
1┌─c1─┐
2│  1 │
3│  2 │
4└────┘
```
Copy command
Great, one row down! Next, we’ll change the table DDL to have an extra column:

```
1ALTER TABLE demo 
2ADD column c2 Nullable(String);
```
Copy command
Query the table again:

```
1┌─c1─┬─c2───┐
2│  1 │ ᴺᵁᴸᴸ │
3│  2 │ ᴺᵁᴸᴸ │
4└────┴──────┘
```
Copy command
Perfect, we have our new column with all null values. Let’s add a new row:

```
1INSERT INTO demo VALUES (4, 'Delta Kernel');
```
Copy command
And then query the table a third time!

```
1┌─c1─┬─c2───────────┐
2│  4 │ Delta Kernel │
3│  1 │ ᴺᵁᴸᴸ         │
4│  2 │ ᴺᵁᴸᴸ         │
5└────┴──────────────┘
```
Copy command
Let’s now update `c2` for the row that has `c1=1`:
