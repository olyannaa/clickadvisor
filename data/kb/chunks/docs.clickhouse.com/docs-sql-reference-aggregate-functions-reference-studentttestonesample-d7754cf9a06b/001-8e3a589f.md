---
source: docs.clickhouse.com
url: https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/sql-reference/aggregate-functions/reference/studentTTestOneSample.md)#
topic: studentttestonesample-clickhouse-docs
ch_version_introduced: '20.0'
last_updated: '2026-06-12'
chunk_index: 1
total_chunks_in_doc: 1
---

# studentTTestOneSample \| ClickHouse Docs

- - [Functions](/docs/sql-reference/functions)- [Aggregate functions](/docs/sql-reference/aggregate-functions)- [Aggregate Functions](/docs/sql-reference/aggregate-functions/reference)- studentTTestOneSample
[Edit this page](https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/sql-reference/aggregate-functions/reference/studentTTestOneSample.md)# studentTTestOneSample

## studentTTestOneSample[​](#studentTTestOneSample "Direct link to studentTTestOneSample")

Introduced in: v25\.10\.0

Applies the one\-sample Student's t\-test to determine whether the mean of a sample differs from a known population mean.

Normality is assumed. The null hypothesis is that the sample mean equals the population mean.

The optional `confidence_level` enables confidence interval calculation.

**Notes:**

- At least 2 observations are required; otherwise the result is `(nan, nan)` (and intervals if requested are `nan`).
- Constant or near\-constant input will also return `nan` due to zero (or effectively zero) standard error.

**See Also**

- [Student's t\-test](https://en.wikipedia.org/wiki/Student%27s_t-test)
- [studentTTest function](/docs/sql-reference/aggregate-functions/reference/studentttest)

**Syntax**

```
studentTTestOneSample([confidence_level])(sample_data, population_mean)

```

**Parameters**

- `confidence_level` — Optional. Confidence level for confidence intervals. Float in (0, 1\). [`Float*`](/docs/sql-reference/data-types/float)

**Arguments**

- `sample_data` — Sample data. [`Integer`](/docs/sql-reference/data-types/int-uint) or [`Float`](/docs/sql-reference/data-types/float) or [`Decimal`](/docs/sql-reference/data-types/decimal)
- `population_mean` — Known population mean to test against (usually a constant). [`(U)Int*`](/docs/sql-reference/data-types/int-uint) or [`Float*`](/docs/sql-reference/data-types/float) or [`Decimal`](/docs/sql-reference/data-types/decimal)

**Returned value**

Returns a tuple with two or four elements (if `confidence_level` is specified): calculated t\-statistic, calculated p\-value (two\-tailed), \[calculated confidence\-interval\-low], \[calculated confidence\-interval\-high]. Confidence intervals are for the sample mean at the given confidence level. [`Tuple(Float64, Float64)`](/docs/sql-reference/data-types/tuple) or [`Tuple(Float64, Float64, Float64, Float64)`](/docs/sql-reference/data-types/tuple)

**Examples**

**Without confidence interval**

```
SELECT studentTTestOneSample()(value, 20.0) FROM t;

```

**With confidence interval (95%)**

```
SELECT studentTTestOneSample(0.95)(value, 20.0) FROM t;

```

**See Also**

- [Student's t\-test](https://en.wikipedia.org/wiki/Student%27s_t-test)
- [studentTTest function](/docs/sql-reference/aggregate-functions/reference/studentttest)
[PreviousstudentTTest](/docs/sql-reference/aggregate-functions/reference/studentttest)[Nextsum](/docs/sql-reference/aggregate-functions/reference/sum)- [studentTTestOneSample](#studentTTestOneSample)
Was this page helpful?
