---
source: docs.clickhouse.com
url: https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/sql-reference/aggregate-functions/reference/exponentialTimeDecayedCount.md)#
topic: exponentialtimedecayedcount-clickhouse-docs
ch_version_introduced: '1.905'
last_updated: '2026-06-12'
chunk_index: 1
total_chunks_in_doc: 2
---

# exponentialTimeDecayedCount \| ClickHouse Docs

- - [Functions](/docs/sql-reference/functions)- [Aggregate functions](/docs/sql-reference/aggregate-functions)- [Aggregate Functions](/docs/sql-reference/aggregate-functions/reference)- exponentialTimeDecayedCount
[Edit this page](https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/sql-reference/aggregate-functions/reference/exponentialTimeDecayedCount.md)# exponentialTimeDecayedCount

## exponentialTimeDecayedCount[​](#exponentialTimeDecayedCount "Direct link to exponentialTimeDecayedCount")

Introduced in: v21\.12\.0

Returns the cumulative exponential decay over a time series at the index `t` in time.

**Syntax**

```
exponentialTimeDecayedCount(x)(t)

```

**Parameters**

- `x` — Half\-life period. [`(U)Int*`](/docs/sql-reference/data-types/int-uint) or [`Float*`](/docs/sql-reference/data-types/float) or [`Decimal`](/docs/sql-reference/data-types/decimal)

**Arguments**

- `t` — Time. [`(U)Int*`](/docs/sql-reference/data-types/int-uint) or [`Float*`](/docs/sql-reference/data-types/float) or [`Decimal`](/docs/sql-reference/data-types/decimal) or [`DateTime`](/docs/sql-reference/data-types/datetime) or [`DateTime64`](/docs/sql-reference/data-types/datetime64)

**Returned value**

Returns the cumulative exponential decay at the given point in time. [`Float64`](/docs/sql-reference/data-types/float)

**Examples**

**Window function usage with visual representation**

```
SELECT
    value,
    time,
    round(exp_smooth, 3),
    bar(exp_smooth, 0, 20, 50) AS bar
FROM
(
    SELECT
        (number % 5) = 0 AS value,
        number AS time,
        exponentialTimeDecayedCount(10)(time) OVER (ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS exp_smooth
    FROM numbers(50)
)

```
