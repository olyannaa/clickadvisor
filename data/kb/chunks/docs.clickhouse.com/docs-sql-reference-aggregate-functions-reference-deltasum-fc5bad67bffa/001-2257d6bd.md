---
source: docs.clickhouse.com
url: https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/sql-reference/aggregate-functions/reference/deltaSum.md)#
topic: deltasum-clickhouse-docs
ch_version_introduced: '2.25'
last_updated: '2026-06-12'
chunk_index: 1
total_chunks_in_doc: 1
---

# deltaSum \| ClickHouse Docs

- - [Functions](/docs/sql-reference/functions)- [Aggregate functions](/docs/sql-reference/aggregate-functions)- [Aggregate Functions](/docs/sql-reference/aggregate-functions/reference)- deltaSum
[Edit this page](https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/sql-reference/aggregate-functions/reference/deltaSum.md)# deltaSum

## deltaSum[​](#deltaSum "Direct link to deltaSum")

Introduced in: v21\.3\.0

Sums the arithmetic difference between consecutive rows.
If the difference is negative, it is ignored.

TipThe underlying data must be sorted for this function to work properly.
If you would like to use this function in a [materialized view](/docs/sql-reference/statements/create/view#materialized-view), you most likely want to use the [`deltaSumTimestamp`](/docs/sql-reference/aggregate-functions/reference/deltasumtimestamp) function instead.

See also:

- [`runningDifference`](/docs/sql-reference/functions/other-functions#runningDifference)

**Syntax**

```
deltaSum(x1[, x2, ...])

```

**Arguments**

- `x1[, x2, ...]` — One or more input values. [`Integer`](/docs/sql-reference/data-types/int-uint) or [`Float`](/docs/sql-reference/data-types/float)

**Returned value**

Returns a gained arithmetic difference of the input values. [`(U)Int*`](/docs/sql-reference/data-types/int-uint) or [`Float*`](/docs/sql-reference/data-types/float)

**Examples**

**Basic usage with positive differences**

```
SELECT deltaSum(arrayJoin([1, 2, 3]))

```

```
┌─deltaSum(arrayJoin([1, 2, 3]))─┐
│                              2 │
└────────────────────────────────┘

```

**Mixed values with negative differences ignored**

```
SELECT deltaSum(arrayJoin([1, 2, 3, 0, 3, 4, 2, 3]))

```

```
┌─deltaSum(arrayJoin([1, 2, 3, 0, 3, 4, 2, 3]))─┐
│                                             7 │
└───────────────────────────────────────────────┘

```

**Float values**

```
SELECT deltaSum(arrayJoin([2.25, 3, 4.5]))

```

```
┌─deltaSum(arrayJoin([2.25, 3, 4.5]))─┐
│                                2.25 │
└─────────────────────────────────────┘

```

**See also**

- [`runningDifference`](/docs/sql-reference/functions/other-functions#runningDifference)
[PreviouscramersVBiasCorrected](/docs/sql-reference/aggregate-functions/reference/cramersvbiascorrected)[NextdeltaSumTimestamp](/docs/sql-reference/aggregate-functions/reference/deltasumtimestamp)- [deltaSum](#deltaSum)
Was this page helpful?
