---
source: docs.clickhouse.com
url: https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/sql-reference/statements/select/array-join.md)#
topic: array-join-clause-clickhouse-docs
ch_version_introduced: auto
last_updated: '2026-06-12'
chunk_index: 6
total_chunks_in_doc: 6
---

│ World │ 5 │ 50 │ [3,4,5] │ [30,40,50] │ └───────┴─────┴─────┴─────────┴────────────┘ ``` Example of using the [arrayEnumerate](/docs/sql-reference/functions/array-functions#arrayEnumerate) function: ``` SELECT s, `n.x`, `n.y`, `nest.x`, `nest.y`, num FROM nested_test ARRAY JOIN nest AS n, arrayEnumerate(`nest.x`) AS num; ```

```
┌─s─────┬─n.x─┬─n.y─┬─nest.x──┬─nest.y─────┬─num─┐
│ Hello │   1 │  10 │ [1,2]   │ [10,20]    │   1 │
│ Hello │   2 │  20 │ [1,2]   │ [10,20]    │   2 │
│ World │   3 │  30 │ [3,4,5] │ [30,40,50] │   1 │
│ World │   4 │  40 │ [3,4,5] │ [30,40,50] │   2 │
│ World │   5 │  50 │ [3,4,5] │ [30,40,50] │   3 │
└───────┴─────┴─────┴─────────┴────────────┴─────┘

```

## Implementation Details[​](#implementation-details "Direct link to Implementation Details")

The query execution order is optimized when running `ARRAY JOIN`. Although `ARRAY JOIN` must always be specified before the [WHERE](/docs/sql-reference/statements/select/where)/[PREWHERE](/docs/sql-reference/statements/select/prewhere) clause in a query, technically they can be performed in any order, unless result of `ARRAY JOIN` is used for filtering. The processing order is controlled by the query optimizer.

### Incompatibility with short\-circuit function evaluation[​](#incompatibility-with-short-circuit-function-evaluation "Direct link to Incompatibility with short-circuit function evaluation")

[Short\-circuit function evaluation](/docs/operations/settings/settings#short_circuit_function_evaluation) is a feature that optimizes the execution of complex expressions in specific functions such as `if`, `multiIf`, `and`, and `or`. It prevents potential exceptions, such as division by zero, from occurring during the execution of these functions.

`arrayJoin` is always executed and not supported for short circuit function evaluation. That's because it's a unique function processed separately from all other functions during query analysis and execution and requires additional logic that doesn't work with short circuit function execution. The reason is that the number of rows in the result depends on the arrayJoin result, and it's too complex and expensive to implement lazy execution of `arrayJoin`.

## Related content[​](#related-content "Direct link to Related content")

- Blog: [Working with time series data in ClickHouse](https://clickhouse.com/blog/working-with-time-series-data-and-functions-ClickHouse)
[PreviousAPPLY](/docs/sql-reference/statements/select/apply-modifier)[NextDISTINCT](/docs/sql-reference/statements/select/distinct)- [Basic ARRAY JOIN Examples](#basic-array-join-examples)
	- [ARRAY JOIN and LEFT ARRAY JOIN](#array-join-left-array-join-examples)- [ARRAY JOIN and arrayEnumerate function](#array-join-arrayEnumerate)- [ARRAY JOIN and arrayEnumerateUniq](#array_join_arrayEnumerateUniq)- [Using Aliases](#using-aliases)- [ARRAY JOIN with Nested Data Structure](#array-join-with-nested-data-structure)- [Implementation Details](#implementation-details)
	- [Incompatibility with short\-circuit function evaluation](#incompatibility-with-short-circuit-function-evaluation)- [Related content](#related-content)
Was this page helpful?
