---
source: docs.clickhouse.com
url: https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/sql-reference/statements/alter/order-by.md)#
topic: manipulating-key-expressions-clickhouse-docs
ch_version_introduced: auto
last_updated: '2026-06-12'
chunk_index: 1
total_chunks_in_doc: 1
---

# Manipulating Key Expressions \| ClickHouse Docs

- - [Introduction](/docs/sql-reference)- [Statements](/docs/sql-reference/statements)- [ALTER](/docs/sql-reference/statements/alter)- ORDER BY
[Edit this page](https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/sql-reference/statements/alter/order-by.md)# Manipulating Key Expressions

```
ALTER TABLE [db].name [ON CLUSTER cluster] MODIFY ORDER BY new_expression

```

The command changes the [sorting key](/docs/engines/table-engines/mergetree-family/mergetree) of the table to `new_expression` (an expression or a tuple of expressions). Primary key remains the same.

The command is lightweight in a sense that it only changes metadata. To keep the property that data part rows are ordered by the sorting key expression you cannot add expressions containing existing columns to the sorting key (only columns added by the `ADD COLUMN` command in the same `ALTER` query, without default column value).

NoteIt only works for tables in the [`MergeTree`](/docs/engines/table-engines/mergetree-family/mergetree) family (including [replicated](/docs/engines/table-engines/mergetree-family/replication) tables).

[PreviousUPDATE](/docs/sql-reference/statements/alter/update)[NextSAMPLE BY](/docs/sql-reference/statements/alter/sample-by)Was this page helpful?
