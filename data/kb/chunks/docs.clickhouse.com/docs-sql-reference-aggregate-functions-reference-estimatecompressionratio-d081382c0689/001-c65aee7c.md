---
source: docs.clickhouse.com
url: https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/sql-reference/aggregate-functions/reference/estimateCompressionRatio.md)#
topic: estimatecompressionratio-clickhouse-docs
ch_version_introduced: '1.9988506608699999'
last_updated: '2026-06-12'
chunk_index: 1
total_chunks_in_doc: 1
---

# estimateCompressionRatio \| ClickHouse Docs

- - [Functions](/docs/sql-reference/functions)- [Aggregate functions](/docs/sql-reference/aggregate-functions)- [Aggregate Functions](/docs/sql-reference/aggregate-functions/reference)- estimateCompressionRatio
[Edit this page](https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/sql-reference/aggregate-functions/reference/estimateCompressionRatio.md)# estimateCompressionRatio

## estimateCompressionRatio[​](#estimateCompressionRatio "Direct link to estimateCompressionRatio")

Introduced in: v25\.4\.0

Estimates the compression ratio of a given column without compressing it.

NoteFor the examples below, the result will differ based on the default compression codec of the server.
See [Column Compression Codecs](/docs/sql-reference/statements/create/table#column_compression_codec).

**Syntax**

```
estimateCompressionRatio([codec, block_size_bytes])(column)

```

**Parameters**

- `codec` — String containing a compression codec or multiple comma\-separated codecs in a single string. [`String`](/docs/sql-reference/data-types/string)
- `block_size_bytes` — Block size of compressed data. This is similar to setting both [`max_compress_block_size`](/docs/operations/settings/merge-tree-settings#max_compress_block_size) and [`min_compress_block_size`](/docs/operations/settings/merge-tree-settings#min_compress_block_size). The default value is 1 MiB (1048576 bytes). Maximum allowed value is 256 MiB (268435456 bytes). [`UInt64`](/docs/sql-reference/data-types/int-uint)

**Arguments**

- `column` — Column of any type. [`Any`](/docs/sql-reference/data-types)

**Returned value**

Returns an estimate compression ratio for the given column. [`Float64`](/docs/sql-reference/data-types/float)

**Examples**

**Basic usage with default codec**

```
CREATE TABLE compression_estimate_example
(
    `number` UInt64
)
ENGINE = MergeTree()
ORDER BY number
SETTINGS min_bytes_for_wide_part = 0;

INSERT INTO compression_estimate_example
SELECT number FROM system.numbers LIMIT 100_000;

SELECT estimateCompressionRatio(number) AS estimate FROM compression_estimate_example

```

```
┌───────────estimate─┐
│ 1.9988506608699999 │
└────────────────────┘

```

**Using a specific codec**

```
SELECT estimateCompressionRatio('T64')(number) AS estimate FROM compression_estimate_example

```

```
┌──────────estimate─┐
│ 3.762758101688538 │
└───────────────────┘

```

**Using multiple codecs**

```
SELECT estimateCompressionRatio('T64, ZSTD')(number) AS estimate FROM compression_estimate_example

```

```
┌───────────estimate─┐
│ 143.60078980434392 │
└────────────────────┘

```
[Previousentropy](/docs/sql-reference/aggregate-functions/reference/entropy)[NextexponentialMovingAverage](/docs/sql-reference/aggregate-functions/reference/exponentialMovingAverage)- [estimateCompressionRatio](#estimateCompressionRatio)
Was this page helpful?
