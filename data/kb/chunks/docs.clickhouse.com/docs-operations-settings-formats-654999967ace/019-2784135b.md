---
source: docs.clickhouse.com
url: https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/operations/settings/settings-formats.md)#
topic: format-settings-clickhouse-docs
ch_version_introduced: '2.00'
last_updated: '2026-06-12'
chunk_index: 19
total_chunks_in_doc: 29
---

to output_format_binary_write_json_as_string") Write values of [JSON](/docs/sql-reference/data-types/newjson) data type as JSON [String](/docs/sql-reference/data-types/string) values in RowBinary output format. ## output\_format\_bson\_string\_as\_string[​](#output_format_bson_string_as_string "Direct link to output_format_bson_string_as_string") Use BSON String type instead of Binary for String columns. ## output\_format\_compression\_level[​](#output_format_compression_level "Direct link to output_format_compression_level")

Default compression level if query output is compressed. The setting is applied when `SELECT` query has `INTO OUTFILE` or when writing to table functions `file`, `url`, `hdfs`, `s3`, or `azureBlobStorage`.

Possible values: from `1` to `22`

## output\_format\_compression\_zstd\_window\_log[​](#output_format_compression_zstd_window_log "Direct link to output_format_compression_zstd_window_log")

Can be used when the output compression method is `zstd`. If greater than `0`, this setting explicitly sets compression window size (power of `2`) and enables a long\-range mode for zstd compression. This can help to achieve a better compression ratio.

Possible values: non\-negative numbers. Note that if the value is too small or too big, `zstdlib` will throw an exception. Typical values are from `20` (window size \= `1MB`) to `30` (window size \= `1GB`).

## output\_format\_csv\_crlf\_end\_of\_line[​](#output_format_csv_crlf_end_of_line "Direct link to output_format_csv_crlf_end_of_line")

If it is set true, end of line in CSV format will be \\r\\n instead of \\n.

## output\_format\_csv\_serialize\_tuple\_into\_separate\_columns[​](#output_format_csv_serialize_tuple_into_separate_columns "Direct link to output_format_csv_serialize_tuple_into_separate_columns")

If it set to true, then Tuples in CSV format are serialized as separate columns (that is, their nesting in the tuple is lost)

## output\_format\_decimal\_trailing\_zeros[​](#output_format_decimal_trailing_zeros "Direct link to output_format_decimal_trailing_zeros")

Output trailing zeros when printing Decimal values. E.g. 1\.230000 instead of 1\.23\.

Disabled by default.

## output\_format\_json\_array\_of\_rows[​](#output_format_json_array_of_rows "Direct link to output_format_json_array_of_rows")

Enables the ability to output all rows as a JSON array in the [JSONEachRow](/docs/interfaces/formats/JSONEachRow) format.

Possible values:

- 1 — ClickHouse outputs all rows as an array, each row in the `JSONEachRow` format.
- 0 — ClickHouse outputs each row separately in the `JSONEachRow` format.

**Example of a query with the enabled setting**

Query:

```
SET output_format_json_array_of_rows = 1;
SELECT number FROM numbers(3) FORMAT JSONEachRow;

```

Result:

```
[
{"number":"0"},
{"number":"1"},
{"number":"2"}
]

```

**Example of a query with the disabled setting**

Query:

```
SET output_format_json_array_of_rows = 0;
SELECT number FROM numbers(3) FORMAT JSONEachRow;

```

Result:

```
{"number":"0"}
{"number":"1"}
{"number":"2"}

```

## output\_format\_json\_escape\_forward\_slashes[​](#output_format_json_escape_forward_slashes "Direct link to output_format_json_escape_forward_slashes")

Controls escaping forward slashes for string outputs in JSON output format. This is intended for compatibility with JavaScript. Don't confuse with backslashes that are always escaped.

Enabled by default.

## output\_format\_json\_map\_as\_array\_of\_tuples[​](#output_format_json_map_as_array_of_tuples "Direct link to output_format_json_map_as_array_of_tuples")
