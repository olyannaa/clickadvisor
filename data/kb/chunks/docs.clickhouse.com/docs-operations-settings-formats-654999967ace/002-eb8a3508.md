---
source: docs.clickhouse.com
url: https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/operations/settings/settings-formats.md)#
topic: format-settings-clickhouse-docs
ch_version_introduced: '2.00'
last_updated: '2026-06-12'
chunk_index: 2
total_chunks_in_doc: 29
---

Simple output format. ClickHouse output date and time `YYYY-MM-DD hh:mm:ss` format. For example, `2019-08-20 10:18:56`. The calculation is performed according to the data type's time zone (if present) or server time zone. - `iso` \- ISO output format.

ClickHouse output date and time in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) `YYYY-MM-DDThh:mm:ssZ` format. For example, `2019-08-20T10:18:56Z`. Note that output is in UTC (`Z` means UTC).
- `unix_timestamp` \- Unix timestamp output format.

ClickHouse output date and time in [Unix timestamp](https://en.wikipedia.org/wiki/Unix_time) format. For example `1566285536`.

See also:

- [DateTime data type.](/docs/sql-reference/data-types/datetime)
- [Functions for working with dates and times.](/docs/sql-reference/functions/date-time-functions)

## date\_time\_overflow\_behavior[​](#date_time_overflow_behavior "Direct link to date_time_overflow_behavior")

Defines the behavior when [Date](/docs/sql-reference/data-types/date), [Date32](/docs/sql-reference/data-types/date32), [DateTime](/docs/sql-reference/data-types/datetime), [DateTime64](/docs/sql-reference/data-types/datetime64) or integers are converted into Date, Date32, DateTime or DateTime64 but the value cannot be represented in the result type.

Possible values:

- `ignore` — Silently ignore overflows. Result are undefined.
- `throw` — Throw an exception in case of overflow.
- `saturate` — Saturate the result. If the value is smaller than the smallest value that can be represented by the target type, the result is chosen as the smallest representable value. If the value is bigger than the largest value that can be represented by the target type, the result is chosen as the largest representable value.

Default value: `ignore`.

## errors\_output\_format[​](#errors_output_format "Direct link to errors_output_format")

Method to write Errors to text output.

## format\_avro\_schema\_registry\_connection\_timeout[​](#format_avro_schema_registry_connection_timeout "Direct link to format_avro_schema_registry_connection_timeout")

For AvroConfluent format: connection timeout in seconds for the Confluent Schema Registry HTTP client. Used by both schema fetch and schema registration. Must be greater than 0 and less than 600 (10 minutes).

## format\_avro\_schema\_registry\_receive\_timeout[​](#format_avro_schema_registry_receive_timeout "Direct link to format_avro_schema_registry_receive_timeout")

For AvroConfluent format: receive timeout in seconds for the Confluent Schema Registry HTTP client. Used by both schema fetch and schema registration. Must be greater than 0 and less than 600 (10 minutes).

## format\_avro\_schema\_registry\_send\_timeout[​](#format_avro_schema_registry_send_timeout "Direct link to format_avro_schema_registry_send_timeout")

For AvroConfluent format: send timeout in seconds for the Confluent Schema Registry HTTP client. Used by both schema fetch and schema registration. Must be greater than 0 and less than 600 (10 minutes).

## format\_avro\_schema\_registry\_url[​](#format_avro_schema_registry_url "Direct link to format_avro_schema_registry_url")

For AvroConfluent format: Confluent Schema Registry URL.

## format\_binary\_max\_array\_size[​](#format_binary_max_array_size "Direct link to format_binary_max_array_size")
