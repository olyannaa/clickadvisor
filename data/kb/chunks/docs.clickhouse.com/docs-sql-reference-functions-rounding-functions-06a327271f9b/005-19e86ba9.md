---
source: docs.clickhouse.com
url: https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/sql-reference/functions/rounding-functions.md)#
topic: rounding-functions-clickhouse-docs
ch_version_introduced: '123.45'
last_updated: '2026-06-12'
chunk_index: 5
total_chunks_in_doc: 6
---

closest from a set of commonly used durations: `1, 10, 30, 60, 120, 180, 240, 300, 600, 1200, 1800, 3600, 7200, 18000, 36000`. If the number is less than one, it returns `0`. **Syntax** ``` roundDuration(num) ``` **Arguments**

- `num` — A number to round to one of the numbers in the set of common durations. [`(U)Int*`](/docs/sql-reference/data-types/int-uint) or [`Float*`](/docs/sql-reference/data-types/float)

**Returned value**

Returns `0`, for `num` \< 1\. Otherwise, one of: `1, 10, 30, 60, 120, 180, 240, 300, 600, 1200, 1800, 3600, 7200, 18000, 36000`. [`UInt16`](/docs/sql-reference/data-types/int-uint)

**Examples**

**Usage example**

```
SELECT *, roundDuration(*) FROM system.numbers WHERE number IN (0, 9, 19, 47, 101, 149, 205, 271, 421, 789, 1423, 2345, 4567, 9876, 24680, 42573)

```

```
┌─number─┬─roundDuration(number)─┐
│      0 │                     0 │
│      9 │                     1 │
│     19 │                    10 │
│     47 │                    30 │
│    101 │                    60 │
│    149 │                   120 │
│    205 │                   180 │
│    271 │                   240 │
│    421 │                   300 │
│    789 │                   600 │
│   1423 │                  1200 │
│   2345 │                  1800 │
│   4567 │                  3600 │
│   9876 │                  7200 │
│  24680 │                 18000 │
│  42573 │                 36000 │
└────────┴───────────────────────┘

```

## roundToExp2[​](#roundToExp2 "Direct link to roundToExp2")

Introduced in: v1\.1\.0

Rounds a number down to the nearest (whole non\-negative) power of two.
If the number is less than one, it returns `0`.

**Syntax**

```
roundToExp2(num)

```

**Arguments**

- `num` — A number to round. [`(U)Int*`](/docs/sql-reference/data-types/int-uint) or [`Float*`](/docs/sql-reference/data-types/float)

**Returned value**

Returns `num` rounded down to the nearest (whole non\-negative) power of two, otherwise `0` for `num < 1`. [`(U)Int*`](/docs/sql-reference/data-types/int-uint) or [`Float*`](/docs/sql-reference/data-types/float)

**Examples**

**Usage example**

```
SELECT *, roundToExp2(*) FROM system.numbers WHERE number IN (0, 2, 5, 10, 19, 50)

```

```
┌─number─┬─roundToExp2(number)─┐
│      0 │                   0 │
│      2 │                   2 │
│      5 │                   4 │
│     10 │                   8 │
│     19 │                  16 │
│     50 │                  32 │
└────────┴─────────────────────┘

```

## trunc[​](#trunc "Direct link to trunc")

Introduced in: v1\.1\.0

Like [`floor`](#floor) but returns the rounded number with the largest absolute value less than or equal to that of `x`.

**Syntax**

```
truncate(x[, N])

```

**Aliases**: `truncate`

**Arguments**
