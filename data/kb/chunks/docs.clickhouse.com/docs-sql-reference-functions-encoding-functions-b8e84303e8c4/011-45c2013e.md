---
source: docs.clickhouse.com
url: https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/sql-reference/functions/encoding-functions.md)#
topic: encoding-functions-clickhouse-docs
ch_version_introduced: '104.1'
last_updated: '2026-06-12'
chunk_index: 11
total_chunks_in_doc: 11
---

binary string (BLOB). If you want to convert the result to a number, you can use the `reverse` and `reinterpretAs<Type>` functions. Note`clickhouse-client` interprets strings as UTF\-8\. This may cause that values returned by `hex` to be displayed surprisingly.

Supports both uppercase and lowercase letters `A-F`.
The number of hexadecimal digits does not have to be even.
If it is odd, the last digit is interpreted as the least significant half of the `00-0F` byte.
If the argument string contains anything other than hexadecimal digits, some implementation\-defined result is returned (an exception isn't thrown).
For a numeric argument the inverse of hex(N) is not performed by unhex().

**Syntax**

```
unhex(arg)

```

**Arguments**

- `arg` — A string containing any number of hexadecimal digits. [`String`](/docs/sql-reference/data-types/string) or [`FixedString`](/docs/sql-reference/data-types/fixedstring)

**Returned value**

Returns a binary string (BLOB). [`String`](/docs/sql-reference/data-types/string)

**Examples**

**Basic usage**

```
SELECT unhex('303132'), UNHEX('4D7953514C')

```

```
┌─unhex('303132')─┬─unhex('4D7953514C')─┐
│ 012             │ MySQL               │
└─────────────────┴─────────────────────┘

```

**Convert to number**

```
SELECT reinterpretAsUInt64(reverse(unhex('FFF'))) AS num

```

```
┌──num─┐
│ 4095 │
└──────┘

```
[PreviousEmbedded dictionary](/docs/sql-reference/functions/ym-dict-functions)[NextEncryption](/docs/sql-reference/functions/encryption-functions)- [bech32Decode](#bech32Decode)- [bech32Encode](#bech32Encode)- [bin](#bin)- [bitPositionsToArray](#bitPositionsToArray)- [bitmaskToArray](#bitmaskToArray)- [bitmaskToList](#bitmaskToList)- [char](#char)- [hex](#hex)- [hilbertDecode](#hilbertDecode)- [hilbertEncode](#hilbertEncode)- [mortonDecode](#mortonDecode)- [mortonEncode](#mortonEncode)- [sqidDecode](#sqidDecode)- [sqidEncode](#sqidEncode)- [unbin](#unbin)- [unhex](#unhex)
Was this page helpful?
