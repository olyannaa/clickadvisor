---
source: docs.clickhouse.com
url: https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/sql-reference/statements/update.md)#
topic: the-lightweight-update-statement-clickhouse-docs
ch_version_introduced: auto
last_updated: '2026-06-12'
chunk_index: 4
total_chunks_in_doc: 4
---

of data parts to execute an update and produce a patch part. Because of that there can be two cases of applying patch parts. For example if we read part `A`, we need to apply patch part `X`:

- if `X` contains part `A` itself. It happens if `A` was not participating in merge when `UPDATE` was executed.
- if `X` contains part `B` and `C`, which are covered by part `A`. It happens if there was a merge (`B`, `C`) \-\> `A` running when `UPDATE` was executed.

For these two cases there are two ways to apply patch parts respectively:

- Using merge by sorted columns `_part`, `_part_offset`.
- Using join by `_block_number`, `_block_offset` columns.

The join mode is slower and requires more memory than the merge mode, but it is used less often.

## Related Content[​](#related-content "Direct link to Related Content")

- [`ALTER UPDATE`](/docs/sql-reference/statements/alter/update) \- Heavy `UPDATE` operations
- [Lightweight `DELETE`](/docs/sql-reference/statements/delete) \- Lightweight `DELETE` operations
- [`APPLY PATCHES`](/docs/sql-reference/statements/alter/apply-patches) \- Force physical materialization of patches to data parts (mutation operation)
[PreviousREVOKE](/docs/sql-reference/statements/revoke)[NextATTACH](/docs/sql-reference/statements/attach)- [Examples](#examples)- [Lightweight updates do not update data immediately](#lightweight-update-does-not-update-data-immediately)- [Lightweight updates requirements](#lightweight-update-requirements)- [Lightweight deletes](#lightweight-delete)- [Performance considerations](#performance-considerations)- [Concurrent operations](#concurrent-operations)- [Update permissions](#update-permissions)- [Details of the implementation](#details-of-the-implementation)- [Related Content](#related-content)
Was this page helpful?
