---
source: docs.clickhouse.com
url: https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/sql-reference/statements/alter/partition.md)#
topic: manipulating-partitions-and-parts-clickhouse-docs
ch_version_introduced: auto
last_updated: '2026-06-12'
chunk_index: 9
total_chunks_in_doc: 9
---

the [ALTER ATTACH PART](#attach-partitionpart) and [DROP DETACHED PART](#drop-detached-partitionpart) query, to specify the name of a part, use string literal with a value from the `name` column of the [system.detached\_parts](/docs/operations/system-tables/detached_parts) table. For example, `ALTER TABLE visits ATTACH PART '201901_1_1_0'`.

Usage of quotes when specifying the partition depends on the type of partition expression. For example, for the `String` type, you have to specify its name in quotes (`'`). For the `Date` and `Int*` types no quotes are needed.

All the rules above are also true for the [OPTIMIZE](/docs/sql-reference/statements/optimize) query. If you need to specify the only partition when optimizing a non\-partitioned table, set the expression `PARTITION tuple()`. For example:

```
OPTIMIZE TABLE table_not_partitioned PARTITION tuple() FINAL;

```

`IN PARTITION` specifies the partition to which the [UPDATE](/docs/sql-reference/statements/alter/update) or [DELETE](/docs/sql-reference/statements/alter/delete) expressions are applied as a result of the `ALTER TABLE` query. New parts are created only from the specified partition. In this way, `IN PARTITION` helps to reduce the load when the table is divided into many partitions, and you only need to update the data point\-by\-point.

The examples of `ALTER ... PARTITION` queries are demonstrated in the tests [`00502_custom_partitioning_local`](https://github.com/ClickHouse/ClickHouse/blob/master/tests/queries/0_stateless/00502_custom_partitioning_local.sql) and [`00502_custom_partitioning_replicated_zookeeper`](https://github.com/ClickHouse/ClickHouse/blob/master/tests/queries/0_stateless/00502_custom_partitioning_replicated_zookeeper.sql).

[PreviousCOLUMN](/docs/sql-reference/statements/alter/column)[NextSETTING](/docs/sql-reference/statements/alter/setting)- [DETACH PARTITION\|PART](#detach-partitionpart)- [DROP PARTITION\|PART](#drop-partitionpart)- [DROP DETACHED PARTITION\|PART](#drop-detached-partitionpart)- [FORGET PARTITION](#forget-partition)- [ATTACH PARTITION\|PART](#attach-partitionpart)- [ATTACH PARTITION FROM](#attach-partition-from)- [REPLACE PARTITION](#replace-partition)- [MOVE PARTITION TO TABLE](#move-partition-to-table)- [CLEAR COLUMN IN PARTITION](#clear-column-in-partition)- [FREEZE PARTITION](#freeze-partition)- [UNFREEZE PARTITION](#unfreeze-partition)- [CLEAR INDEX IN PARTITION](#clear-index-in-partition)- [FETCH PARTITION\|PART](#fetch-partitionpart)- [MOVE PARTITION\|PART](#move-partitionpart)- [UPDATE IN PARTITION](#update-in-partition)
	- [Example](#example)- [See Also](#see-also)- [DELETE IN PARTITION](#delete-in-partition)
	- [Example](#example-1)- [REWRITE PARTS](#rewrite-parts)
	- [Example](#example-rewrite-parts)- [See Also](#see-also-1)- [How to Set Partition Expression](#how-to-set-partition-expression)
Was this page helpful?
