---
source: docs.clickhouse.com
url: https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/engines/table-engines/mergetree-family/mergetree.md)#
topic: mergetree-table-engine-clickhouse-docs
ch_version_introduced: '0.025'
last_updated: '2026-06-12'
chunk_index: 14
total_chunks_in_doc: 31
---

13 │ └──────────────────────────┘ ``` Of course, you can also use those functions to estimate parameters for other conditions. The functions above refer to the bloom filter calculator [here](https://hur.st/bloomfilter). #### Token bloom filter[​](#token-bloom-filter "Direct link to Token bloom filter")

NoteWith general availability (GA) of the `text` index starting from ClickHouse version 26\.2, the `tokenbf_v1` index is no longer recommended for full text search.See page ["Full\-text search with text indexes"](/docs/engines/table-engines/mergetree-family/textindexes) for details.

```
tokenbf_v1(size_of_bloom_filter_in_bytes, number_of_hash_functions, random_seed)

```

#### Sparse grams bloom filter[​](#sparse-grams-bloom-filter "Direct link to Sparse grams bloom filter")

The sparse grams bloom filter is similar to `ngrambf_v1` but uses [sparse grams tokens](/docs/sql-reference/functions/string-functions#sparseGrams) instead of ngrams.

```
sparse_grams(min_ngram_length, max_ngram_length, min_cutoff_length, size_of_bloom_filter_in_bytes, number_of_hash_functions, random_seed)

```

### Text index[​](#text "Direct link to Text index")

Builds an inverted index over tokenized string data, enabling efficient and deterministic full\-text search. See [here](/docs/engines/table-engines/mergetree-family/textindexes) for details.

#### Vector similarity[​](#vector-similarity "Direct link to Vector similarity")

Supports approximate nearest neighbor search, see [here](/docs/engines/table-engines/mergetree-family/annindexes) for details.

### Functions support[​](#functions-support "Direct link to Functions support")

Conditions in the `WHERE` clause contains calls of the functions that operate with columns. If the column is a part of an index, ClickHouse tries to use this index when performing the functions. ClickHouse supports different subsets of functions for using indexes.

Indexes of type `set` can be utilized by all functions. The other index types are supported as follows:
