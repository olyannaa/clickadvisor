---
source: docs.clickhouse.com
url: https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/sql-reference/functions/random-functions.md)#
topic: functions-for-generating-random-numbers-clickhouse-docs
ch_version_introduced: '0.0'
last_updated: '2026-06-12'
chunk_index: 3
total_chunks_in_doc: 8
---

elimination](/docs/sql-reference/functions/overview#common-subexpression-elimination) when the same function call is used multiple times in a query. [`Any`](/docs/sql-reference/data-types) **Returned value** Returns a random Float64 number drawn from the specified binomial distribution. [`UInt64`](/docs/sql-reference/data-types/int-uint) **Examples** **Usage example** ``` SELECT randBinomial(100, .75) FROM numbers(5) ```

```
┌─randBinomial(100, 0.75)─┐
│                      74 │
│                      78 │
│                      76 │
│                      77 │
│                      80 │
└─────────────────────────┘

```

## randCanonical[​](#randCanonical "Direct link to randCanonical")

Introduced in: v22\.11\.0

Returns a random distributed `Float64` number with uniform distribution between `0` (inclusive) and `1` (exclusive).

**Syntax**

```
randCanonical([x])

```

**Arguments**

- `x` — Optional and ignored. The only purpose of the argument is to prevent [common subexpression elimination](/docs/sql-reference/functions/overview#common-subexpression-elimination) when the same function call is used multiple times in a query. [`Any`](/docs/sql-reference/data-types)

**Returned value**

Returns a random Float64 number. [`Float64`](/docs/sql-reference/data-types/float)

**Examples**

**Usage example**

```
SELECT randCanonical();

```

```
0.345217890123456

```

## randChiSquared[​](#randChiSquared "Direct link to randChiSquared")

Introduced in: v22\.10\.0

Returns a random Float64 number drawn from a [chi\-square distribution](https://en.wikipedia.org/wiki/Chi-squared_distribution).

**Syntax**

```
randChiSquared(degree_of_freedom[, x])

```

**Arguments**

- `degree_of_freedom` — Degrees of freedom. [`Float64`](/docs/sql-reference/data-types/float)
- `x` — Optional and ignored. The only purpose of the argument is to prevent [common subexpression elimination](/docs/sql-reference/functions/overview#common-subexpression-elimination) when the same function call is used multiple times in a query. [`Any`](/docs/sql-reference/data-types)

**Returned value**

Returns a random Float64 number drawn from the specified chi\-square distribution. [`Float64`](/docs/sql-reference/data-types/float)

**Examples**

**Usage example**

```
SELECT randChiSquared(10) FROM numbers(5)

```

```
┌─randChiSquared(10)─┐
│ 10.015463656521543 │
│  9.621799919882768 │
│   2.71785015634699 │
│ 11.128188665931908 │
│  4.902063104425469 │
└────────────────────┘

```

## randConstant[​](#randConstant "Direct link to randConstant")

Introduced in: v1\.1\.0

Generates a single random value that remains constant across all rows in the current query execution.

This function:

- Returns the same random value for every row within a single query
- Produces different values across separate query executions

It is useful for applying consistent random seeds or identifiers across all rows in a dataset

**Syntax**

```
randConstant([x])

```

**Arguments**

- `x` — Optional and ignored. The only purpose of the argument is to prevent [common subexpression elimination](/docs/sql-reference/functions/overview#common-subexpression-elimination) when the same function call is used multiple times in a query. [`Any`](/docs/sql-reference/data-types)

**Returned value**

Returns a column of type `UInt32` containing the same random value in each row. [`UInt32`](/docs/sql-reference/data-types/int-uint)

**Examples**

**Basic usage**

```
SELECT randConstant() AS random_value;

```

```
| random_value |
|--------------|
| 1234567890   |

```

**Usage with parameter**
