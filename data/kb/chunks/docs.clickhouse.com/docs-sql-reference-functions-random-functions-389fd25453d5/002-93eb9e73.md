---
source: docs.clickhouse.com
url: https://github.com/ClickHouse/ClickHouse/tree/master/docs/en/sql-reference/functions/random-functions.md)#
topic: functions-for-generating-random-numbers-clickhouse-docs
ch_version_introduced: '0.0'
last_updated: '2026-06-12'
chunk_index: 2
total_chunks_in_doc: 8
---

**Returned value** Returns a random number of type `UInt32`. [`UInt32`](/docs/sql-reference/data-types/int-uint) **Examples** **Usage example** ``` SELECT rand(); ``` ``` 1569354847 ``` ## rand64[​](#rand64 "Direct link to rand64") Introduced in: v1\.1\.0 Returns a random distributed `UInt64` number with uniform distribution.

Uses a linear congruential generator with an initial state obtained from the system, which means that while it appears random, it's not truly random and can be predictable if the initial state is known.
For scenarios where true randomness is crucial, consider using alternative methods like system\-level calls or integrating with external libraries.

**Syntax**

```
rand64([x])

```

**Arguments**

- `x` — Optional and ignored. The only purpose of the argument is to prevent [common subexpression elimination](/docs/sql-reference/functions/overview#common-subexpression-elimination) when the same function call is used multiple times in a query. [`Any`](/docs/sql-reference/data-types)

**Returned value**

Returns a random UInt64 number with uniform distribution. [`UInt64`](/docs/sql-reference/data-types/int-uint)

**Examples**

**Usage example**

```
SELECT rand64();

```

```
15030268859237645412

```

## randBernoulli[​](#randBernoulli "Direct link to randBernoulli")

Introduced in: v22\.10\.0

Returns a random Float64 number drawn from a [Bernoulli distribution](https://en.wikipedia.org/wiki/Bernoulli_distribution).

**Syntax**

```
randBernoulli(probability[, x])

```

**Arguments**

- `probability` — The probability of success as a value between `0` and `1`. [`Float64`](/docs/sql-reference/data-types/float)
- `x` — Optional and ignored. The only purpose of the argument is to prevent [common subexpression elimination](/docs/sql-reference/functions/overview#common-subexpression-elimination) when the same function call is used multiple times in a query. [`Any`](/docs/sql-reference/data-types)

**Returned value**

Returns a random Float64 number drawn from the specified Bernoulli distribution. [`UInt64`](/docs/sql-reference/data-types/int-uint)

**Examples**

**Usage example**

```
SELECT randBernoulli(.75) FROM numbers(5)

```

```
┌─randBernoulli(0.75)─┐
│                   1 │
│                   1 │
│                   0 │
│                   1 │
│                   1 │
└─────────────────────┘

```

## randBinomial[​](#randBinomial "Direct link to randBinomial")

Introduced in: v22\.10\.0

Returns a random Float64 number drawn from a [binomial distribution](https://en.wikipedia.org/wiki/Binomial_distribution).

**Syntax**

```
randBinomial(experiments, probability[, x])

```

**Arguments**

- `experiments` — The number of experiments [`UInt64`](/docs/sql-reference/data-types/int-uint)
- `probability` — The probability of success in each experiment as a value between `0` and `1` [`Float64`](/docs/sql-reference/data-types/float)
- `x` — Optional and ignored. The only purpose of the argument is to prevent [common subexpression elimination](/docs/sql-reference/functions/overview#common-subexpression-elimination) when the same function call is used multiple times in a query. [`Any`](/docs/sql-reference/data-types)

**Returned value**

Returns a random Float64 number drawn from the specified binomial distribution. [`UInt64`](/docs/sql-reference/data-types/int-uint)

**Examples**

**Usage example**

```
SELECT randBinomial(100, .75) FROM numbers(5)

```
