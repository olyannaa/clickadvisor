# ClickAdvisor benchmark case template.
# Keep expert labels conservative. If a field is not yet reviewed with a DBA,
# leave it in TODO state instead of guessing.

status: proposed # TODO: set reviewed after manual rule-by-rule verification
case_id: "CASE-XXX"
source: "TODO: tpch | clickbench | job | synthetic | github-issues"
sql: |
  -- TODO: paste SQL here
schema_files: [] # TODO: add relative schema file paths if relevant
schema_ddl: null # TODO: inline DDL for schema-aware cases when no schema file is needed
environment: null # TODO: structured environment context for env-aware cases
context_type: sql-only # sql-only | schema-aware | env-aware | explain-aware
known_issues: [] # TODO: list of {issue_id, type, detected_by_rule, severity, description}
expected_rules_to_fire: [] # TODO: expert-only labeling with DBA review
expected_findings_count: 0 # TODO: expected number of findings in the final report
expected_improvement: null # TODO: qualitative expectation such as low/medium/high
synthetic_explain_path: null # TODO: relative path if a synthetic explain artifact exists
notes: |
  TODO: free-form notes, caveats, or dataset provenance.
